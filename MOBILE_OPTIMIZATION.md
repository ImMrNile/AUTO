# 📱 Оптимизация для мобильных устройств

## 🔥 Найденные проблемы

### 1. Множественный polling (постоянные запросы к серверу)
**Проблема:** Несколько компонентов постоянно опрашивают сервер, даже когда пользователь не активен.

**Найдено:**
- `TaskNotificationsGlobal.tsx` - опрос каждые 50 секунд
- `TaskResetButton.tsx` - опрос каждые 30 секунд
- `SinglePageProductForm.tsx` - опрос каждые 50 секунд
- `telegram-desktop/page.tsx` - опрос каждые 2 секунды (!)

**Влияние:**
- Постоянная работа процессора
- Расход батареи
- Нагрев устройства
- Расход трафика

### 2. Фоновые обновления кеша
**Проблема:** `useProductsCache` и `useAnalyticsCache` автоматически обновляют данные в фоне.

**Влияние:**
- Дополнительные запросы к API
- Обработка больших объемов данных
- Нагрузка на память

### 3. Симуляция прогресса
**Проблема:** `InfographicCreator.tsx` использует `setInterval` для симуляции прогресса.

**Влияние:**
- Постоянные перерисовки компонента
- Лишняя работа процессора

## ✅ Решения

### 1. Адаптивный polling с учетом активности
```typescript
// Увеличиваем интервал когда вкладка неактивна
const getPollingInterval = () => {
  if (document.hidden) {
    return 120000; // 2 минуты для неактивной вкладки
  }
  return 50000; // 50 секунд для активной
};
```

### 2. Отключение polling на мобильных
```typescript
const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
const pollingInterval = isMobile ? 120000 : 50000; // Реже на мобильных
```

### 3. Использование Page Visibility API
```typescript
useEffect(() => {
  const handleVisibilityChange = () => {
    if (document.hidden) {
      // Останавливаем polling
      clearInterval(intervalRef.current);
    } else {
      // Возобновляем polling
      startPolling();
    }
  };
  
  document.addEventListener('visibilitychange', handleVisibilityChange);
  return () => document.removeEventListener('visibilitychange', handleVisibilityChange);
}, []);
```

### 4. Отключение фоновых обновлений на мобильных
```typescript
const config = {
  backgroundRefresh: !isMobile, // Отключаем на мобильных
  cacheTime: isMobile ? 10 * 60 * 1000 : 5 * 60 * 1000 // Дольше кешируем на мобильных
};
```

### 5. Debounce и throttle для событий
```typescript
import { debounce } from 'lodash';

const debouncedUpdate = debounce(() => {
  // Обновление данных
}, 1000);
```

### 6. Lazy loading компонентов
```typescript
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <div>Загрузка...</div>,
  ssr: false // Не рендерим на сервере
});
```

### 7. Виртуализация списков
Для длинных списков товаров используйте виртуализацию:
```typescript
import { FixedSizeList } from 'react-window';
```

## 🔧 Приоритетные исправления

### Высокий приоритет:
1. ✅ Увеличить интервалы polling с 2-50 секунд до 2-5 минут
2. ✅ Добавить Page Visibility API для остановки polling
3. ✅ Отключить фоновые обновления на мобильных

### Средний приоритет:
4. Добавить debounce для пользовательских действий
5. Lazy loading тяжелых компонентов
6. Виртуализация списков

### Низкий приоритет:
7. Service Worker для кеширования
8. Оптимизация изображений (WebP, lazy loading)
9. Code splitting

## 📊 Мониторинг производительности

### Добавьте метрики:
```typescript
// Время загрузки компонента
const startTime = performance.now();
// ... ваш код
const endTime = performance.now();
console.log(`Компонент загружен за ${endTime - startTime}ms`);

// Использование памяти (только для разработки)
if (process.env.NODE_ENV === 'development') {
  console.log('Memory:', performance.memory);
}
```

### Chrome DevTools:
1. **Performance tab** - записывайте профиль на мобильном устройстве
2. **Memory tab** - проверяйте утечки памяти
3. **Network tab** - смотрите количество запросов
4. **Lighthouse** - проверяйте производительность

## 🎯 Ожидаемый результат

После оптимизации:
- ✅ Снижение нагрузки на процессор на 70-80%
- ✅ Уменьшение количества запросов в 5-10 раз
- ✅ Отсутствие нагрева устройства
- ✅ Быстрая загрузка страниц
- ✅ Плавная работа интерфейса

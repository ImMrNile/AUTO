# 🧠 Умная система получения данных оптимизации

## Проблема

**Обычная система:**
- Запрашивает данные за последнюю неделю
- Если товара не было в наличии → пустой массив ❌
- Если не было кампаний → пустой массив ❌
- **Результат:** AI не может анализировать, нет данных

**Ваш случай:**
- Товара не было 2 месяца
- Кампании были только для других товаров категории
- Нужны данные для AI анализа

## ✅ Решение: Умная система поиска

### Стратегия 1: Поиск в прошлом

Система **автоматически ищет неделя за неделей назад** пока не найдет период с активностью:

```
Неделя 1 (29.10-04.11): нет активности ⚠️
Неделя 2 (22.10-28.10): нет активности ⚠️
Неделя 3 (15.10-21.10): нет активности ⚠️
Неделя 4 (08.10-14.10): нет активности ⚠️
Неделя 5 (01.10-07.10): ЕСТЬ активность! ✅
  - 42 заказа
  - 228 в корзину
  - 4713 просмотров
Неделя 6 (24.09-30.09): ЕСТЬ активность! ✅
  - 38 заказов
  - 210 в корзину
  - 4520 просмотров
...
Продолжает пока не соберет 60 дней данных
```

### Стратегия 2: Анализ связанных кампаний

**Не только кампании для товара, но и:**
1. Кампании для других товаров **той же категории**
2. Кампании где товар мог получать конверсию (объединенные карточки)
3. Ключевые слова из всех кампаний категории

```typescript
// Фильтруем кампании:
const relevantCampaigns = campaigns.filter((c: any) => {
  const nms = c.autoParams?.nms || [];
  const hasOurProduct = nms.includes(nmId);  // 1. Наш товар
  const subjectMatch = c.autoParams?.subject?.id === subjectId;  // 2. Та же категория
  return hasOurProduct || subjectMatch;  // ← Берем ОБА варианта!
});
```

### Стратегия 3: Данные категории (fallback)

Если **вообще нет данных** для товара → берем данные категории:
- Топ-100 поисковых запросов категории
- Топ-20 конкурентов
- Ключевые слова из кампаний категории

## 📡 API Endpoint

### GET `/api/products/[id]/smart-optimization-data`

**Автоматически:**
1. Ищет данные неделя за неделей назад (до 12 недель)
2. Собирает минимум 60 дней данных
3. Включает кампании категории
4. Если нет данных → переключается на анализ категории

## 📊 Что собирает

### 1. Поисковые запросы
- ✅ Запросы для товара
- ✅ Запросы категории (если нет для товара)
- ✅ Частота, конверсия, позиции

### 2. Данные конверсии
- ✅ Просмотры, корзина, заказы, выкупы
- ✅ CTR, конверсия, процент выкупа
- ✅ Агрегация за весь период

### 3. Рекламные кампании
- ✅ Кампании с вашим товаром
- ✅ Кампании категории (связанные)
- ✅ Статистика: клики, показы, CPC, ROI

### 4. Ключевые слова
- ✅ Из кампаний товара
- ✅ Из кампаний категории
- ✅ Конверсия, позиции, ставки

### 5. Воронка продаж
- ✅ По дням за весь период
- ✅ Просмотры → корзина → заказ → выкуп

## 🎯 Формат ответа

### Успешный поиск (стратегия: historical)

```json
{
  "success": true,
  "strategy": "historical",
  "message": "Данные собраны за 60 дней из 8 недель",
  "weeksSearched": 8,
  "dataPoints": 60,
  "data": {
    "searchQueries": {
      "total": 145,
      "queries": [
        {
          "text": "платье мусульманское",
          "frequency": 1250,
          "orders": 42,
          "conversion": 3.36
        },
        ...
      ]
    },
    "conversion": {
      "totalViews": 32450,
      "totalCart": 1820,
      "totalOrders": 285,
      "totalBuyouts": 178,
      "ctr": "5.61",
      "conversion": "15.66",
      "buyoutRate": "62.46"
    },
    "campaigns": {
      "total": 5,
      "stats": [...]
    },
    "keywords": {
      "total": 87,
      "keywords": [
        {
          "keyword": "платье умра",
          "views": 5420,
          "clicks": 320,
          "orders": 15,
          "cpc": 45
        },
        ...
      ]
    },
    "salesFunnel": {
      "totalDays": 60,
      "history": [...]
    }
  }
}
```

### Fallback на категорию (стратегия: category)

```json
{
  "success": true,
  "strategy": "category",
  "message": "Данные получены из анализа категории (товар не имеет собственной истории)",
  "category": {
    "id": 69,
    "name": "Платья"
  },
  "data": {
    "searchQueries": [
      // Топ-100 запросов категории
    ],
    "topCompetitors": [
      // Топ-20 товаров категории
    ],
    "categoryKeywords": [
      // Ключевые слова из кампаний категории
    ]
  }
}
```

## 🔍 Как работает поиск

### Проверка активности

Система считает что есть активность если:
```typescript
const hasActivity = 
  searchQueries.length > 0 ||  // Есть поисковые запросы
  orders > 0 ||                // Есть заказы
  views > 0 ||                 // Есть просмотры
  campaigns.length > 0 ||      // Есть кампании
  funnelData.some(h => h.openCount > 0);  // Есть данные воронки
```

### Логика сбора

```typescript
while (weeksSearched < 12 && totalDataPoints < 60) {
  // 1. Запрашиваем данные за неделю
  const weekData = await fetchWeekData(...);
  
  // 2. Проверяем есть ли активность
  if (hasActivity(weekData)) {
    // 3. Добавляем к собранным данным
    collectedData.push(weekData);
    totalDataPoints += 7;
  }
  
  // 4. Переходим к следующей неделе назад
  weeksSearched++;
  
  // 5. Задержка 2 секунды
  await sleep(2000);
}
```

## 💡 Примеры использования

### Ваш случай: Платье (нет в наличии 2 месяца)

**Запрос:**
```bash
GET /api/products/cmh0gghd5002bunv4jxmcdfue/smart-optimization-data
```

**Что произойдет:**
1. Неделя 1 (29.10-04.11): нет активности
2. Неделя 2 (22.10-28.10): нет активности
3. Неделя 3 (15.10-21.10): нет активности
4. Неделя 4 (08.10-14.10): нет активности
5. **Неделя 5 (01.10-07.10): НАЙДЕНА АКТИВНОСТЬ!**
   - 42 заказа
   - Кампании категории "Платья"
   - Ключевые слова: "платье мусульманское", "хиджаб платье"
6. Продолжает сбор до 60 дней

**Результат:**
```json
{
  "strategy": "historical",
  "weeksSearched": 8,
  "dataPoints": 60,
  "data": {
    "searchQueries": { "total": 145 },
    "conversion": { "totalOrders": 285 },
    "campaigns": { "total": 5 },  // ← Включая кампании категории!
    "keywords": { "total": 87 }   // ← Из всех кампаний категории!
  }
}
```

### Новый товар (только создан)

**Запрос:**
```bash
GET /api/products/new-product-id/smart-optimization-data
```

**Что произойдет:**
1. Проверяет 12 недель назад
2. Нигде нет активности
3. **Переключается на стратегию category**
4. Получает данные категории

**Результат:**
```json
{
  "strategy": "category",
  "message": "Данные получены из анализа категории",
  "data": {
    "searchQueries": [
      // Топ-100 запросов категории "Наушники"
    ],
    "topCompetitors": [
      // Топ-20 наушников
    ],
    "categoryKeywords": [
      // Ключевые слова из кампаний категории
    ]
  }
}
```

## 🤖 Для AI анализа

Теперь AI **всегда** получает данные:

### Сценарий 1: Товар с историей
```json
{
  "searchQueries": 145 запросов,
  "conversion": {
    "totalOrders": 285,
    "ctr": "5.61%",
    "conversion": "15.66%"
  },
  "keywords": 87 ключевых слов,
  "campaigns": 5 кампаний
}
```

**AI может:**
- ✅ Найти лучшие ключевые слова
- ✅ Определить оптимальную цену
- ✅ Выявить проблемы конверсии
- ✅ Дать конкретные рекомендации

### Сценарий 2: Новый товар
```json
{
  "categoryQueries": 100 запросов категории,
  "topCompetitors": 20 конкурентов,
  "categoryKeywords": 150 ключевых слов
}
```

**AI может:**
- ✅ Предложить ключевые слова из категории
- ✅ Рекомендовать цену по конкурентам
- ✅ Показать популярные запросы
- ✅ Дать стратегию запуска

## ⏱️ Время выполнения

**Формула:** `(недель × 2 секунды) + время запросов`

**Примеры:**
- 2 недели назад нашли данные: ~4-6 секунд
- 5 недель назад нашли данные: ~10-15 секунд
- 8 недель назад нашли данные: ~16-20 секунд
- 12 недель, нет данных → категория: ~24-30 секунд

## 📈 Логи в консоли

```
🧠 [Smart Optimization] Умный поиск данных для товара: cmh0gghd5002bunv4jxmcdfue
📊 [Smart Optimization] Товар: 493805274, Категория: Платья (69)
🔍 [Smart Optimization] Начинаем поиск данных (цель: 60 дней)

📅 [Smart Optimization] Неделя 1: 2025-10-29 - 2025-11-04
   🔍 Поисковые запросы: 0
   📊 Конверсия: заказов 0
   ⚠️ Поисковые запросы: ошибка
   ⚠️ Кампании: ошибка
⚠️ [Smart Optimization] Нет активности в неделе 1

📅 [Smart Optimization] Неделя 2: 2025-10-22 - 2025-10-28
   🔍 Поисковые запросы: 0
   📊 Конверсия: заказов 0
⚠️ [Smart Optimization] Нет активности в неделе 2

...

📅 [Smart Optimization] Неделя 5: 2025-10-01 - 2025-10-07
   🔍 Поисковые запросы: 23
   📊 Конверсия: заказов 42
   📢 Кампании: 3 (включая категорию)
   🛒 Воронка: 7 дней
   🔑 Ключевые слова: 15
✅ [Smart Optimization] Найдена активность в неделе 5

...

📊 [Smart Optimization] Поиск завершен:
   Недель проверено: 8
   Дней данных собрано: 60
```

## ✅ Результат

Теперь система **ВСЕГДА** возвращает данные для AI:

1. **Товар с историей** → исторические данные за период активности
2. **Товар без истории** → данные категории и конкурентов
3. **Кампании категории** → включены автоматически
4. **Ключевые слова** → из всех связанных кампаний

**AI получает полный датасет для анализа и может дать конструктивные рекомендации!** 🚀

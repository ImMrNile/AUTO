# Исправление ошибки подключения к Supabase

## Проблема
```
Can't reach database server at aws-1-eu-north-1.pooler.supabase.com:6543
```

## Причина
Prisma Client не может подключиться к базе данных. Возможные причины:
1. Prisma Client устарел или поврежден
2. Next.js dev server держит файлы Prisma заблокированными
3. Временные проблемы с сетью

## Решение

### Шаг 1: Остановите dev сервер
В терминале где запущен `npm run dev`, нажмите `Ctrl+C`

### Шаг 2: Перегенерируйте Prisma Client
```bash
npx prisma generate
```

### Шаг 3: Перезапустите dev сервер
```bash
npm run dev
```

### Альтернативное решение (если не помогло)

1. **Очистите кеш Prisma:**
```bash
# Остановите dev сервер
# Удалите папку с Prisma Client
Remove-Item -Recurse -Force node_modules\.prisma

# Перегенерируйте
npx prisma generate

# Перезапустите
npm run dev
```

2. **Проверьте подключение к Supabase:**
```bash
Test-NetConnection -ComputerName aws-1-eu-north-1.pooler.supabase.com -Port 6543
```
Должно быть: `TcpTestSucceeded : True`

3. **Проверьте DATABASE_URL в .env:**
```
DATABASE_URL="postgresql://postgres.skjdonmsvmxlsnkhgftz:GPilCXpmPzolKLFg@aws-1-eu-north-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
```

## Проверка статуса Supabase

Откройте: https://status.supabase.com/
Убедитесь что все сервисы работают (зеленые галочки)

## Если ничего не помогло

1. **Перезагрузите компьютер** - иногда помогает сбросить сетевые подключения
2. **Проверьте Firewall** - убедитесь что порт 6543 не заблокирован
3. **Проверьте VPN** - если используете VPN, попробуйте отключить/включить
4. **Используйте Direct URL** - измените в `.env`:
```
DATABASE_URL="postgresql://postgres.skjdonmsvmxlsnkhgftz:GPilCXpmPzolKLFg@aws-1-eu-north-1.pooler.supabase.com:5432/postgres"
```
(порт 5432 вместо 6543, без pgbouncer)

## Результат

После перегенерации Prisma Client и перезапуска dev сервера, ошибка должна исчезнуть:
```
✅ [AuthService] Найдена сессия Supabase, ищем пользователя в БД...
🧠 [Smart Optimization] Умный поиск данных для товара: cmh2798e60001unmw72e1417c
```

## Примечание

Эта ошибка **не связана** с исправлениями рекламных кампаний. Она возникает на этапе авторизации, до того как код дойдет до запросов WB API.

После исправления подключения к БД, система сможет:
1. Авторизовать пользователя
2. Получить данные товара
3. Запросить данные кампаний (включая тип 9)
4. Вернуть результат оптимизации

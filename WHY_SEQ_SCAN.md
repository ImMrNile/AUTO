# Почему PostgreSQL использует Seq Scan вместо Index Scan?

## Проблема

После создания индексов PostgreSQL все равно использует **Seq Scan** (последовательное сканирование):

```
-> Seq Scan on product_creation_tasks (cost=0.00..1.21 rows)
   Filter: (("userId" = 'cmhcbit860000ib04m8im7vfz')::text
   Rows Removed by Filter: 13
```

**Execution Time: 0.098 ms** - быстро, но не использует индекс!

---

## Причины

### 1. Маленькая таблица

Если в таблице мало строк (< 1000), PostgreSQL считает что **Seq Scan быстрее**:
- Seq Scan: читает всю таблицу последовательно (0.098ms)
- Index Scan: читает индекс + таблицу (может быть медленнее)

**Ваш случай:** 13 строк в таблице → Seq Scan эффективнее!

### 2. Неправильный порядок колонок в индексе

**Было:**
```sql
CREATE INDEX ON product_creation_tasks (userId, status, createdAt DESC);
```

**Проблема:** PostgreSQL не может эффективно использовать индекс для запроса:
```sql
WHERE userId = ? AND status IN (?, ?, ?) ORDER BY createdAt DESC
```

**Почему?** 
- `status IN (...)` - это не точное равенство, а диапазон
- Индекс не может эффективно фильтровать по диапазону в середине

### 3. Статистика таблицы

PostgreSQL использует статистику для выбора плана:
- Если статистика устарела → неправильный план
- Если таблица маленькая → Seq Scan предпочтительнее

---

## Решение

### Вариант 1: Подождать роста таблицы

Когда в таблице будет > 1000 строк, PostgreSQL автоматически начнет использовать индекс.

**Текущая ситуация:**
- 13 строк в таблице
- Seq Scan: 0.098ms
- Index Scan: ~0.150ms (медленнее!)

**При 1000+ строк:**
- Seq Scan: ~50ms
- Index Scan: ~2ms (быстрее!)

### Вариант 2: Оптимизировать индекс

**Правильный порядок колонок:**
```sql
CREATE INDEX ON product_creation_tasks (userId, createdAt DESC, status);
```

**Почему лучше?**
1. `userId` - точное равенство (высокая селективность)
2. `createdAt DESC` - сортировка (используется в ORDER BY)
3. `status` - фильтр (используется в WHERE)

### Вариант 3: Частичный индекс

Создать индекс только для активных задач:

```sql
CREATE INDEX ON product_creation_tasks (userId, createdAt DESC)
WHERE status IN ('CREATING', 'ANALYZING', 'PUBLISHING');
```

**Преимущества:**
- Меньше размер индекса
- Быстрее обновление
- Точнее статистика

---

## Что делать сейчас?

### ✅ Ничего не делать!

**Причина:** Таблица маленькая (13 строк), Seq Scan работает отлично (0.098ms).

**Когда исправлять?**
- Когда в таблице будет > 1000 строк
- Когда Seq Scan станет медленным (> 10ms)
- Когда увидите "⚠️ SSE: Медленный запрос" в логах

### 📊 Мониторинг

Следите за логами:
```
📊 SSE: Задач - активных: X, завершенных: Y (XXms)
```

**Если время < 100ms** - все отлично, ничего не нужно менять!

### 🔧 Если нужно оптимизировать сейчас

Выполните в Supabase SQL Editor:

```sql
-- Удаляем старые индексы
DROP INDEX IF EXISTS "product_creation_tasks_userId_status_idx";
DROP INDEX IF EXISTS "product_creation_tasks_userId_status_createdAt_idx";

-- Создаем оптимальный индекс
CREATE INDEX "product_creation_tasks_userId_createdAt_status_idx" 
ON "product_creation_tasks" ("userId", "createdAt" DESC, "status");

-- Обновляем статистику
ANALYZE "product_creation_tasks";
```

---

## Вывод

**Текущая ситуация нормальная!** ✅

- Execution Time: **0.098ms** - очень быстро
- Seq Scan эффективен для маленьких таблиц
- Индекс начнет использоваться автоматически при росте данных

**Не нужно ничего исправлять, пока запросы быстрые!**

---

## Дополнительно: Как заставить использовать индекс?

Если очень хочется увидеть Index Scan (для тестирования):

```sql
-- Временно отключить Seq Scan
SET enable_seqscan = OFF;

-- Проверить план
EXPLAIN ANALYZE
SELECT * FROM "product_creation_tasks"
WHERE "userId" = 'cmhcbit860000ib04m8im7vfz'
AND "status" IN ('CREATING', 'ANALYZING', 'PUBLISHING')
ORDER BY "createdAt" DESC
LIMIT 10;

-- Включить обратно
SET enable_seqscan = ON;
```

**Но это только для тестирования!** В production PostgreSQL сам выберет оптимальный план.

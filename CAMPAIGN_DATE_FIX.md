# Исправление ошибки "Invalid begin date" при запросе статистики кампаний

## Проблема

При запросе статистики кампаний через `/adv/v3/fullstats` получали ошибку 400:

```
❌ Кампании (fullstats): 400 {
  "detail": "Invalid begin date",
  "origin": "camp-api-public-cache",
  "status": 400,
  "title": "invalid payload"
}
```

**Запрос:**
```
GET /adv/v3/fullstats?from=2025-10-09&to=2025-10-15&ids=27673276,20377168
```

## Причина

API `/adv/v3/fullstats` требует чтобы **запрашиваемый период был ВНУТРИ диапазона активности кампании**.

**Две проблемы:**

### 1. Период вне диапазона активности
- Кампания 27673276: активна с 2025-10-20 по 2025-11-05
- Запрашиваем период: 2025-10-09 - 2025-10-15
- **Результат:** Ошибка! Период начинается ДО старта кампании

### 2. Дефолтные даты окончания (2100-01-01)
- Кампания 27673276: `end=2100-01-01T00:00:00+03:00`
- Кампания 20377168: `end=2100-01-01T00:00:00+03:00`
- **Проблема:** Это дефолтная дата для бесконечных кампаний (статус 11 = на паузе)
- **Результат:** API не принимает такие далекие даты!

**Текущая фильтрация:**
```typescript
// Проверяли только пересечение периодов
if (campaignStart && campaignStart > periodEnd) return false; // Еще не началась
if (campaignEnd && campaignEnd < periodStart) return false; // Уже закончилась
```

Это правильно фильтровало кампании, но **не корректировало даты запроса**.

## Решение

Добавлена **корректировка дат запроса** с обработкой дефолтных дат:

```typescript
if (relevantCampaigns.length > 0) {
  let adjustedStartDate = startDate;
  let adjustedEndDate = endDate;
  
  // Максимальная дата для запроса - сегодня (API не принимает будущие даты)
  const today = new Date().toISOString().split('T')[0];
  
  relevantCampaigns.forEach((c: any) => {
    if (c.startTime) {
      const campaignStart = c.startTime.split('T')[0];
      if (campaignStart > adjustedStartDate) {
        adjustedStartDate = campaignStart; // Сдвигаем начало вперед
      }
    }
    if (c.endTime) {
      const campaignEnd = c.endTime.split('T')[0];
      
      // Игнорируем дефолтные даты окончания (2100-01-01 и подобные)
      const endYear = parseInt(campaignEnd.split('-')[0]);
      if (endYear > 2030) {
        console.log(`Кампания ${c.advertId}: игнорируем дефолтную дату ${campaignEnd}`);
        return; // Пропускаем эту дату
      }
      
      if (campaignEnd < adjustedEndDate) {
        adjustedEndDate = campaignEnd; // Сдвигаем конец назад
      }
    }
  });
  
  // Ограничиваем конечную дату сегодняшним днем
  if (adjustedEndDate > today) {
    console.log(`Ограничиваем дату ${adjustedEndDate} сегодняшним днем: ${today}`);
    adjustedEndDate = today;
  }
  
  // Проверяем валидность
  if (adjustedStartDate > adjustedEndDate) {
    console.log(`⚠️ Нет общего периода активности`);
  } else {
    const statsUrl = `/adv/v3/fullstats?from=${adjustedStartDate}&to=${adjustedEndDate}&ids=${campaignIds}`;
    const statsResponse = await fetch(statsUrl, ...);
  }
}
```

## Логика корректировки

### 1. Находим общий период активности

**Для каждой кампании:**
- Если `startTime` > `adjustedStartDate` → сдвигаем начало вперед
- Если `endTime` < `adjustedEndDate` → сдвигаем конец назад
- **НО:** Игнорируем дефолтные даты (год > 2030) - это бесконечные кампании

### 2. Ограничиваем сегодняшним днем

API не принимает будущие даты, поэтому:
```typescript
if (adjustedEndDate > today) {
  adjustedEndDate = today;
}
```

**Результат:** Период который точно находится внутри диапазона ВСЕХ кампаний и не в будущем

### 2. Проверяем валидность

Если после корректировки `adjustedStartDate > adjustedEndDate` - значит у кампаний нет общего периода активности.

**Пример:**
- Кампания 1: 2025-10-01 - 2025-10-15
- Кампания 2: 2025-10-20 - 2025-10-31
- **Результат:** Нет пересечения, статистику не запрашиваем

### 3. Логируем изменения

Если период был скорректирован - выводим информацию:
```
📅 Период скорректирован: 2025-10-09..2025-10-15 → 2025-10-20..2025-10-15
```

## Примеры работы

### Пример 1: Кампания началась позже

**Исходные данные:**
- Запрашиваемый период: 2025-10-09 - 2025-10-15
- Кампания 27673276: старт 2025-10-20

**Корректировка:**
```
adjustedStartDate = max(2025-10-09, 2025-10-20) = 2025-10-20
adjustedEndDate = min(2025-10-15, null) = 2025-10-15
```

**Проверка:** 2025-10-20 > 2025-10-15 ❌  
**Результат:** Нет общего периода, запрос не выполняется

### Пример 2: Кампания закончилась раньше

**Исходные данные:**
- Запрашиваемый период: 2025-10-20 - 2025-11-10
- Кампания 27673276: конец 2025-11-05

**Корректировка:**
```
adjustedStartDate = max(2025-10-20, 2025-10-20) = 2025-10-20
adjustedEndDate = min(2025-11-10, 2025-11-05) = 2025-11-05
```

**Проверка:** 2025-10-20 <= 2025-11-05 ✅  
**Результат:** Запрашиваем период 2025-10-20 - 2025-11-05

### Пример 3: Несколько кампаний

**Исходные данные:**
- Запрашиваемый период: 2025-10-01 - 2025-11-30
- Кампания 1: 2025-10-10 - 2025-10-25
- Кампания 2: 2025-10-20 - 2025-11-05

**Корректировка:**
```
adjustedStartDate = max(2025-10-01, 2025-10-10, 2025-10-20) = 2025-10-20
adjustedEndDate = min(2025-11-30, 2025-10-25, 2025-11-05) = 2025-10-25
```

**Результат:** Запрашиваем период 2025-10-20 - 2025-10-25 (общее пересечение)

## Логирование

**До исправления:**
```
📤 Запрос статистики кампаний: 2 шт (IDs: 27673276,20377168)
❌ Кампании (fullstats): 400 {"detail":"Invalid begin date"}
```

**После исправления:**
```
📤 Запрос статистики кампаний: 2 шт (IDs: 27673276,20377168)
📅 Период скорректирован: 2025-10-09..2025-10-15 → 2025-10-20..2025-10-15
⚠️ Нет общего периода активности для всех кампаний (2025-10-20 > 2025-10-15)
```

Или если есть общий период:
```
📤 Запрос статистики кампаний: 2 шт (IDs: 27673276,20377168)
📅 Период скорректирован: 2025-10-01..2025-11-30 → 2025-10-20..2025-10-25
📢 Статистика кампаний получена: 2 кампаний
   Кампания 27673276: просмотры=1500, клики=75, заказы=8, CPM=500
   Кампания 20377168: просмотры=2000, клики=100, заказы=10, CPM=450
```

## Преимущества

✅ **Нет ошибок 400** - период всегда валиден  
✅ **Прозрачность** - видно как корректируется период  
✅ **Безопасность** - проверка валидности перед запросом  
✅ **Гибкость** - работает с любым количеством кампаний  

## Файлы изменены

- `src/app/api/products/[id]/smart-optimization-data/route.ts`
  - Строки 819-875: Добавлена корректировка дат запроса статистики кампаний
  - Строки 840-841: Проверка валидности скорректированного периода
  - Строки 847-849: Логирование корректировки

## Тестирование

1. Запустите умный поиск для товара с кампаниями
2. Проверьте логи:
   - Если период скорректирован - должно быть сообщение `📅 Период скорректирован`
   - Если нет общего периода - `⚠️ Нет общего периода активности`
   - Если период валиден - `📢 Статистика кампаний получена`
3. Убедитесь что ошибка 400 "Invalid begin date" больше не появляется

## Альтернативное решение

Вместо корректировки периода для всех кампаний можно запрашивать статистику **для каждой кампании отдельно** с её собственным периодом активности. Но это:
- ❌ Больше запросов к API
- ❌ Медленнее
- ❌ Выше риск rate limit

Текущее решение оптимальнее - один запрос для всех кампаний с общим периодом.

# 🚀 ЗАПУСТИТЕ ЭТО

## ✅ Улучшенный скрипт исправления

```bash
node fix-product-cabinets-v2.mjs
```

## 🔧 Что исправлено

### Проблема в старом скрипте:
- ❌ Пытался создать дубликаты связей
- ❌ Не проверял существующие связи
- ❌ Ошибка: `Unique constraint failed`

### Новый скрипт:
- ✅ Проверяет существующие связи
- ✅ Пропускает дубликаты
- ✅ Удаляет неправильные связи
- ✅ Создает только правильные связи
- ✅ Работает только с опубликованными товарами (wbNmId)

---

## 📊 Что делает скрипт

1. **Находит опубликованные товары** (с wbNmId)
2. **Проверяет привязку к кабинету:**
   - Есть ли связь?
   - Есть ли у кабинета токен?
   - Активен ли кабинет?
3. **Исправляет проблемы:**
   - Удаляет неправильные связи
   - Создает правильные связи
   - Пропускает уже правильные

---

## 🎯 Ожидаемый результат

```
📦 Найдено опубликованных товаров: 9

✅ Набор инструментов
   Привязан к кабинету: BASS
   nmID: 593236888

✅ Крабик для волос
   Привязан к кабинету: BASS
   nmID: 589654166

...

📊 РЕЗУЛЬТАТ:
✅ Уже правильно настроено: 0
🔧 Исправлено: 9
❌ Ошибок: 0
📦 Всего проверено: 9

🎉 Готово к тестированию: 9 товаров
```

---

## 🔍 Проверка после исправления

```bash
node check-wb-token.mjs
```

Должно показать:
```
✅ Готовы к тестированию: 9 товаров
```

---

## 🧪 Тестирование

```
http://localhost:3000/test-ai-optimization
```

Выберите товар → Нажмите кнопку → Данные загрузятся! ✅

---

## ⚠️ Если все еще ошибка

### Проверьте что товар опубликован:
```sql
SELECT id, name, "wbNmId", status 
FROM "Product" 
WHERE "wbNmId" IS NOT NULL
LIMIT 10;
```

### Проверьте привязку:
```sql
SELECT 
    p.name,
    p."wbNmId",
    c.name as cabinet_name,
    c."apiToken" IS NOT NULL as has_token
FROM "Product" p
LEFT JOIN "ProductCabinet" pc ON p.id = pc."productId"
LEFT JOIN "Cabinet" c ON pc."cabinetId" = c.id
WHERE p."wbNmId" IS NOT NULL
LIMIT 10;
```

---

## 🚀 ЗАПУСТИТЕ СЕЙЧАС

```bash
node fix-product-cabinets-v2.mjs
```

Это исправит все правильно! 🎉

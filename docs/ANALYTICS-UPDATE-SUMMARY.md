# Обновление аналитики - Реальные расчеты Wildberries

## Что было исправлено

### ✅ Корректная схема расчетов WB

Реализована **РЕАЛЬНАЯ** схема расчетов согласно работе Wildberries:

```
1. priceWithDisc (что заплатил покупатель)
   ↓
2. - Скидка WB (SPP) → компенсирует Wildberries
   ↓
3. = finishedPrice (БАЗА продавца - от этой суммы идут все расчеты)
   ↓
4. - Комиссия WB (5-20% в зависимости от категории)
   ↓
5. - Логистика (доставка до клиента + возвраты)
   ↓
6. - Хранение (за время на складе WB)
   ↓
7. - Приемка (приемка товара на склад)
   ↓
8. - Прочие вычеты (штрафы, корректировки)
   ↓
9. = forPay (К ПЕРЕВОДУ продавцу)
   ↓
10. - Себестоимость товара
   ↓
11. = ЧИСТАЯ ПРИБЫЛЬ
```

### 📊 Обновленные файлы

#### 1. **Dashboard API** (`src/app/api/analytics/dashboard/route.ts`)

**Изменения:**
- ✅ Выручка = `finishedPrice` (база продавца, а не `priceWithDisc`)
- ✅ Расчет ВСЕХ реальных расходов WB: комиссия + логистика + хранение + приемка + прочие вычеты
- ✅ Формула: `forPay = finishedPrice - комиссия - логистика - хранение - приемка - прочее`
- ✅ Прибыль = `forPay - себестоимость`
- ✅ Добавлено поле `totalOtherDeductions` для штрафов и корректировок
- ✅ Подробное логирование всех расчетов

**Ключевые метрики:**
```typescript
totalRevenue = sum(finishedPrice)  // База продавца
totalForPay = sum(forPay)          // К переводу
totalProfit = totalForPay - totalCost  // Чистая прибыль
profitMargin = (totalProfit / totalRevenue) * 100  // Маржа
```

#### 2. **Финансовый калькулятор** (`lib/services/wbFinancialCalculator.ts`)

**Изменения:**
- ✅ Добавлено поле `priceWithWbDiscount` для цены покупателя
- ✅ Добавлено поле `wbDiscount` для скидки WB (SPP)
- ✅ Добавлено поле `customerPrice` в результатах расчета
- ✅ Маржа теперь рассчитывается относительно цены покупателя
- ✅ Добавлено поле `netProfit` для совместимости

#### 3. **Компонент дашборда** (`src/app/components/AnalyticsDashboard.tsx`)

**Изменения:**
- ✅ Обновлен интерфейс с полем `totalOtherDeductions`
- ✅ Улучшено отображение расходов WB с детализацией
- ✅ Добавлен блок "Расчет прибыли (схема WB)" с пошаговым расчетом
- ✅ Показываются все вычеты: комиссия, логистика, хранение, приемка, прочие
- ✅ Наглядная визуализация: База → Вычеты WB → К переводу → Себестоимость → Прибыль

#### 4. **Документация** (`docs/WB-FINANCIAL-CALCULATIONS.md`)

**Создан новый документ** с полным описанием:
- Структура данных из WB API
- Формулы расчетов
- Примеры с реальными цифрами
- Частые ошибки и правильный подход
- Проверка расчетов

## Ключевые принципы

### 1. База расчетов = finishedPrice

```typescript
// ✅ ПРАВИЛЬНО
const revenue = sale.finishedPrice;  // База продавца
```

```typescript
// ❌ НЕПРАВИЛЬНО
const revenue = sale.priceWithDisc;  // Это цена для покупателя
```

### 2. Скидка WB НЕ является расходом продавца

```typescript
// Скидка WB компенсируется Wildberries
const wbDiscount = priceWithDisc - finishedPrice;
// Это НЕ вычитается из прибыли продавца!
```

### 3. Все расходы WB вычитаются из finishedPrice

```typescript
const wbExpenses = finishedPrice - forPay;
// Включает: комиссию + логистику + хранение + приемку + прочее
```

### 4. Прибыль = К переводу - Себестоимость

```typescript
const profit = forPay - costPrice;
// Налоги платятся отдельно, не из forPay
```

## Отображение для пользователя

### Блок "Детализация расходов WB"

```
Вычеты из базы продавца (finishedPrice):
├─ Комиссия WB: 15,000₽ (15%)
├─ Логистика: 10,000₽ (10%)
├─ Хранение: 2,000₽ (2%)
├─ Приёмка: 500₽ (0.5%)
├─ Прочие вычеты: 500₽
└─ Всего вычетов WB: 28,000₽ (28%)
```

### Блок "Расчет прибыли (схема WB)"

```
База продавца: 100,000₽
- Вычеты WB: -28,000₽
─────────────────────
= К переводу: 72,000₽
- Себестоимость: -40,000₽
─────────────────────
= Чистая прибыль: 32,000₽
💡 Маржа: 32%
```

## Преимущества новой реализации

### ✅ Точность
- Используются реальные данные из WB API
- Учитываются все вычеты: комиссия, логистика, хранение, приемка, штрафы
- Формула соответствует реальной схеме работы WB

### ✅ Прозрачность
- Пользователь видит ВСЕ расходы
- Понятная структура: база → вычеты → к переводу → прибыль
- Детализация каждого вычета с процентами

### ✅ Корректность
- Скидка WB не учитывается как расход продавца
- Налоги показываются отдельно (платятся не из forPay)
- Себестоимость берется только из БД (если указана)

## Проверка корректности

### Формула проверки:

```typescript
finishedPrice - комиссия - логистика - хранение - приемка - прочее = forPay
```

### Пример:

```typescript
finishedPrice: 1,145₽
- комиссия (15%): -171.75₽
- логистика: -100₽
- хранение: -20₽
- приемка: -5₽
- прочее: -10₽
─────────────────────
= forPay: 838.25₽ ✅
```

## Следующие шаги

### Рекомендации для улучшения:

1. **Интеграция с API рекламы WB**
   - Получать реальные расходы на рекламу
   - Добавлять их к расходам продавца

2. **Детализация логистики**
   - Разделить на: доставка до клиента + возвраты
   - Показывать отдельно стоимость возвратов

3. **Учет налогов**
   - Рассчитывать налоги от forPay
   - Показывать чистую прибыль после налогов (опционально)

4. **История изменений**
   - Отслеживать изменения комиссий WB
   - Показывать динамику расходов

## Заключение

Реализована **корректная** схема расчетов, полностью соответствующая реальной работе Wildberries:

- ✅ **finishedPrice** - база продавца
- ✅ **Все вычеты WB** рассчитываются из реальных данных API
- ✅ **forPay** = finishedPrice - все вычеты WB
- ✅ **Прибыль** = forPay - себестоимость
- ✅ **Прозрачное отображение** всех расходов для пользователя

Пользователи теперь видят **РЕАЛЬНЫЕ** затраты и могут принимать обоснованные решения на основе точных данных.

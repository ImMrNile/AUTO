# 🧠 AI Память для кампаний - Полная настройка

## 📋 Обзор

Система автоматического управления кампаниями с **персистентной памятью** для каждой кампании. AI помнит всю историю решений и может управлять кампанией на протяжении недель/месяцев.

---

## 🎯 Как это работает

### Архитектура:

```
┌─────────────────────────────────────────────────────────────┐
│  Товар: Платье с химаром (nmID: 356956444)                 │
│  └─ Кампания: 27673276                                      │
│     └─ OpenAI Thread: thread_abc123 (ПАМЯТЬ AI)            │
│        │                                                     │
│        ├─ 09.11 12:00 - Создание кампании                  │
│        │  User: "Создай кампанию для товара"               │
│        │  AI: "Создал кампанию 27673276, добавил 3 ключа"  │
│        │                                                     │
│        ├─ 09.11 15:00 - Первая проверка (через 3 часа)     │
│        │  User: "Статистика: 50 показов, 2 клика, 0 заказов"│
│        │  AI: "CTR 4% - низко. Увеличиваю ставку на 20%"   │
│        │                                                     │
│        ├─ 09.11 18:00 - Вторая проверка                    │
│        │  User: "Статистика: 80 показов, 8 кликов, 1 заказ"│
│        │  AI: "CTR 10% - отлично! Оставляем как есть"      │
│        │                                                     │
│        ├─ 09.11 21:00 - Третья проверка                    │
│        │  User: "120 показов, 15 кликов, 2 заказа"         │
│        │  AI: "ROI 250% - увеличиваю бюджет на 50%"        │
│        │                                                     │
│        └─ 10.11 00:00 - Четвертая проверка                 │
│           User: "150 показов, 20 кликов, 3 заказа"         │
│           AI: "Все отлично! Продолжаем в том же духе"      │
└─────────────────────────────────────────────────────────────┘
```

**Ключевая особенность:** AI помнит ВСЕ предыдущие решения и видит динамику изменений!

---

## 🚀 Настройка

### Шаг 1: Создайте OpenAI Assistant

1. Перейдите на https://platform.openai.com/assistants
2. Нажмите **"Create Assistant"**
3. Заполните:
   - **Name:** `WB Campaign Manager`
   - **Instructions:** Скопируйте содержимое файла `DEVELOPER_MESSAGE_GPT5.txt`
   - **Model:** `gpt-4-turbo-preview` или `gpt-4o`
   - **Tools:** Включите `Code Interpreter` (опционально)
4. Нажмите **"Save"**
5. Скопируйте **Assistant ID** (например: `asst_abc123`)

### Шаг 2: Добавьте переменные окружения

Откройте `.env.local` и добавьте:

```env
# OpenAI API
OPENAI_API_KEY=sk-proj-...
OPENAI_ASSISTANT_ID=asst_abc123

# Cron Secret (для защиты endpoint)
CRON_SECRET=your-random-secret-key-here
```

### Шаг 3: Примените миграцию базы данных

```bash
# Применить миграцию
psql -U postgres -d your_database -f migrations/add_ai_campaign_memory.sql

# Или через Prisma (если используете)
npx prisma db push
```

### Шаг 4: Обновите Prisma Schema

Добавьте в `prisma/schema.prisma`:

```prisma
model Campaign {
  id              Int       @id @default(autoincrement())
  wbCampaignId    Int?
  name            String
  status          String    @default("draft")
  
  // AI поля
  aiThreadId      String?   // ID Thread в OpenAI
  aiEnabled       Boolean   @default(false)
  lastAiCheckAt   DateTime?
  
  // Связи
  product         Product   @relation(fields: [productId], references: [id])
  productId       Int
  
  decisions       CampaignDecision[]
  actionLogs      CampaignActionLog[]
  
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

model CampaignDecision {
  id          Int       @id @default(autoincrement())
  campaignId  Int
  diagnosis   String
  actions     Json
  forecast    Json?
  executed    Boolean   @default(false)
  executedAt  DateTime?
  createdAt   DateTime  @default(now())
  
  campaign    Campaign  @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  actionLogs  CampaignActionLog[]
  
  @@index([campaignId, createdAt(sort: Desc)])
}

model CampaignActionLog {
  id            Int       @id @default(autoincrement())
  campaignId    Int
  decisionId    Int?
  actionType    String
  actionDetails Json
  status        String    @default("pending") // pending, success, failed
  result        Json?
  error         String?
  createdAt     DateTime  @default(now())
  executedAt    DateTime?
  
  campaign      Campaign          @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  decision      CampaignDecision? @relation(fields: [decisionId], references: [id], onDelete: SetNull)
  
  @@index([campaignId, createdAt(sort: Desc)])
  @@index([status])
}
```

Затем:
```bash
npx prisma generate
npx prisma db push
```

### Шаг 5: Установите зависимости

```bash
npm install openai@^4.0.0
```

### Шаг 6: Деплой на Vercel

```bash
# Добавьте переменные окружения в Vercel
vercel env add OPENAI_API_KEY
vercel env add OPENAI_ASSISTANT_ID
vercel env add CRON_SECRET

# Деплой
vercel --prod
```

---

## 📊 Использование

### 1. Включить AI для кампании

```typescript
// В вашем коде
await prisma.campaign.update({
  where: { id: campaignId },
  data: { aiEnabled: true }
});
```

### 2. Ручная проверка кампании

```bash
# Через API
curl -X POST https://your-domain.com/api/campaigns/27673276/auto-check \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. Автоматическая проверка (Cron)

Cron Job запускается **автоматически каждые 3 часа** (настроено в `vercel.json`):

```
00:00 - Проверка всех активных кампаний
03:00 - Проверка всех активных кампаний
06:00 - Проверка всех активных кампаний
09:00 - Проверка всех активных кампаний
12:00 - Проверка всех активных кампаний
15:00 - Проверка всех активных кампаний
18:00 - Проверка всех активных кампаний
21:00 - Проверка всех активных кампаний
```

### 4. Просмотр истории решений AI

```bash
# Через API
curl https://your-domain.com/api/campaigns/27673276/auto-check \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Ответ:
```json
{
  "success": true,
  "campaignId": 27673276,
  "history": [
    {
      "role": "user",
      "content": "Создай кампанию для товара...",
      "createdAt": "2025-11-09T12:00:00Z"
    },
    {
      "role": "assistant",
      "content": "{\"diagnosis\": \"...\", \"actions\": [...]}",
      "createdAt": "2025-11-09T12:00:15Z"
    },
    {
      "role": "user",
      "content": "Статистика: 50 показов, 2 клика...",
      "createdAt": "2025-11-09T15:00:00Z"
    },
    {
      "role": "assistant",
      "content": "{\"diagnosis\": \"CTR 4% - низко...\", \"actions\": [...]}",
      "createdAt": "2025-11-09T15:00:12Z"
    }
  ],
  "total": 8
}
```

---

## 🔧 Настройка расписания

### Изменить частоту проверки

Откройте `vercel.json` и измените `schedule`:

```json
{
  "crons": [
    {
      "path": "/api/cron/check-campaigns",
      "schedule": "0 */5 * * *"  // Каждые 5 часов
    }
  ]
}
```

**Форматы расписания:**
- `0 */3 * * *` - Каждые 3 часа (в 00:00, 03:00, 06:00, ...)
- `0 */5 * * *` - Каждые 5 часов (в 00:00, 05:00, 10:00, ...)
- `0 0,6,12,18 * * *` - В 00:00, 06:00, 12:00, 18:00
- `*/30 * * * *` - Каждые 30 минут

---

## 📈 Пример работы AI с памятью

### День 1 (09.11.2025)

**12:00 - Создание кампании**
```
User: Создай кампанию для товара "Платье с химаром"
AI: ✅ Создал кампанию 27673276
    - Добавил 3 ключа: "намазник", "химар", "мусульманское платье"
    - Ставки: 100₽, 90₽, 85₽
    - Бюджет: 1500₽/день
```

**15:00 - Первая проверка**
```
User: Статистика за 3 часа:
      - Показы: 50
      - Клики: 2
      - Заказы: 0
      - Затраты: 180₽

AI: ⚠️ CTR 4% - ниже нормы (цель >8%)
    Действия:
    1. Увеличиваю ставку "намазник" с 100₽ до 120₽ (+20%)
    2. Увеличиваю ставку "химар" с 90₽ до 110₽ (+22%)
    Прогноз: CTR вырастет до 6-8% через 3 часа
```

**18:00 - Вторая проверка**
```
User: Статистика за 3 часа:
      - Показы: 80
      - Клики: 8
      - Заказы: 1
      - Затраты: 320₽

AI: ✅ CTR 10% - отлично! (было 4%, стало 10%)
    ✅ Первый заказ получен!
    Действия:
    1. Оставляем ставки без изменений
    2. Мониторим ROI
    Прогноз: Ожидаем 2-3 заказа в следующие 3 часа
```

**21:00 - Третья проверка**
```
User: Статистика за 3 часа:
      - Показы: 120
      - Клики: 15
      - Заказы: 2
      - Затраты: 480₽
      - Выручка: 1200₽

AI: 🎉 ROI 250% - отлично!
    ✅ CTR стабильный 12.5%
    ✅ Конверсия клик→заказ 13.3%
    Действия:
    1. Увеличиваю бюджет с 1500₽ до 2250₽ (+50%)
    2. Добавляю 2 новых ключа: "платье макси", "закрытое платье"
    Прогноз: +3-5 заказов/день при текущем ROI
```

### День 2 (10.11.2025)

**00:00 - Ночная проверка**
```
User: Статистика за 3 часа:
      - Показы: 180
      - Клики: 22
      - Заказы: 3
      - Затраты: 720₽
      - Выручка: 1800₽

AI: ✅ ROI 250% - стабильно
    ✅ CTR 12.2% - стабильно
    Действия:
    1. Все показатели в норме
    2. Продолжаем без изменений
    Прогноз: Стабильные 8-12 заказов/день
```

**Ключевое преимущество:** AI видит, что CTR вырос с 4% до 12% после увеличения ставок в 15:00, и понимает, что это сработало!

---

## 🎛️ Настройка автоматического выполнения

### Автоматическое выполнение критических действий

В файле `lib/ai/campaign-assistant.ts` уже настроено:

```typescript
// Если AI предложил критические действия - выполняем автоматически
const criticalActions = advice.actions.filter(
  a => a.priority === 'critical' && a.autoExecutable
);

if (criticalActions.length > 0) {
  for (const action of criticalActions) {
    await executeAction(campaignId, action);
  }
}
```

**Критические действия** (выполняются автоматически):
- `PAUSE_CAMPAIGN` - если ROI < 50%
- `ADD_MINUS_WORDS` - если нерелевантные запросы
- `CHANGE_BID` - если CPC слишком высокий

**Некритические действия** (требуют подтверждения):
- `UPDATE_PRODUCT_NAME`
- `UPDATE_PRODUCT_DESCRIPTION`
- `CREATE_CAMPAIGN`

---

## 📊 Мониторинг

### Просмотр логов Cron Job

```bash
# В Vercel Dashboard
vercel logs --follow

# Или через API
curl https://your-domain.com/api/cron/check-campaigns \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

### Статистика работы AI

```sql
-- Количество решений AI по кампании
SELECT 
  c.id,
  c.name,
  COUNT(cd.id) as decisions_count,
  MAX(cd.createdAt) as last_decision
FROM "Campaign" c
LEFT JOIN "CampaignDecision" cd ON c.id = cd.campaignId
WHERE c.aiEnabled = true
GROUP BY c.id, c.name;

-- Количество выполненных действий
SELECT 
  actionType,
  status,
  COUNT(*) as count
FROM "CampaignActionLog"
GROUP BY actionType, status;
```

---

## ⚠️ Важные замечания

### 1. Стоимость OpenAI API

- **GPT-4 Turbo:** ~$0.01 за 1K токенов (input) + $0.03 за 1K токенов (output)
- **Средний запрос:** ~5K токенов input + 2K токенов output = ~$0.11
- **8 проверок/день:** ~$0.88/день на кампанию
- **30 дней:** ~$26.4/месяц на кампанию

**Оптимизация:**
- Проверяйте только активные кампании
- Используйте `gpt-4o-mini` для экономии (в 10 раз дешевле)
- Отключайте AI для неэффективных кампаний

### 2. Rate Limits OpenAI

- **Tier 1:** 500 запросов/день
- **Tier 2:** 5,000 запросов/день
- **Tier 3:** 10,000 запросов/день

**Решение:** Добавьте задержку между проверками кампаний (уже реализовано: 2 секунды).

### 3. Безопасность

- ✅ Cron endpoint защищен секретным ключом
- ✅ Thread ID привязан к кампании (нельзя получить чужую историю)
- ✅ Все действия логируются в `CampaignActionLog`

---

## 🔄 Альтернативные решения

### Вариант 2: Langchain + Vector DB (для очень длинной истории)

Если история кампании >100 сообщений, используйте:
- **Langchain** для управления памятью
- **Pinecone/Weaviate** для хранения векторов
- **Суммаризация** старых сообщений

### Вариант 3: Custom Memory (без OpenAI Assistants)

Храните всю историю в PostgreSQL и передавайте последние N сообщений в каждом запросе:

```typescript
const history = await getLastNMessages(campaignId, 10);
const prompt = `
История:
${history.map(m => `${m.role}: ${m.content}`).join('\n')}

Новые данные:
${newData}

Что делать дальше?
`;
```

---

## 📝 Чеклист запуска

- [ ] Создан OpenAI Assistant
- [ ] Добавлены переменные окружения (`OPENAI_API_KEY`, `OPENAI_ASSISTANT_ID`, `CRON_SECRET`)
- [ ] Применена миграция БД (`add_ai_campaign_memory.sql`)
- [ ] Обновлена Prisma Schema
- [ ] Установлена библиотека `openai`
- [ ] Настроен Vercel Cron (`vercel.json`)
- [ ] Деплой на Vercel
- [ ] Включен AI для тестовой кампании (`aiEnabled = true`)
- [ ] Проверен первый запуск Cron Job
- [ ] Проверена история решений AI

---

## 🎯 Готово!

Теперь AI будет **автоматически управлять вашими кампаниями** с полной памятью всех предыдущих решений! 🚀

**Вопросы?** Смотрите примеры в `lib/ai/campaign-assistant.ts`

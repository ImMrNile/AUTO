# 🚀 Фоновые задачи и уведомления

## Что было добавлено

### 1. **Модальное окно успеха после публикации** ✅

После успешной публикации товара на Wildberries пользователь видит красивое модальное окно с тремя кнопками:

- **Создать новый товар** - очищает форму и позволяет сразу создать следующий товар
- **Перейти к товарам** - переключает на вкладку "Товары" для просмотра всех товаров
- **Закрыть** - просто закрывает модальное окно

### 2. **Система фоновых задач** ✅

Теперь пользователь может:
- Создавать несколько товаров параллельно
- Переключаться между вкладками во время создания товара
- Видеть прогресс создания каждого товара в реальном времени

### 3. **Уведомления о статусе задач** ✅

В правом нижнем углу появляется панель уведомлений, которая показывает:

**Активные задачи:**
- Название товара
- Текущий статус (Создание товара... / Анализ ИИ... / Публикация на WB...)
- Прогресс-бар с процентами

**Завершенные задачи:**
- Успешно созданные товары (зеленая плашка)
- Товары с ошибками (красная плашка)
- Кнопка для открытия товара
- Кнопка для удаления уведомления

## Архитектура

### Компоненты

1. **`TaskNotifications.tsx`** - UI компонент для отображения уведомлений
   - Расположение: `src/app/components/BackgroundTasks/TaskNotifications.tsx`
   - Показывает список активных и завершенных задач
   - Поддерживает сворачивание/разворачивание

2. **`useBackgroundTasks.ts`** - React хук для управления задачами
   - Расположение: `src/app/components/BackgroundTasks/useBackgroundTasks.ts`
   - Функции: `addTask`, `updateTask`, `removeTask`, `completeTask`, `errorTask`

3. **`page.tsx`** - Главная страница с интеграцией фоновых задач
   - Использует хук `useBackgroundTasks`
   - Передает функции управления задачами в `SinglePageProductForm`

4. **`SinglePageProductForm.tsx`** - Форма создания товара
   - Получает функции управления задачами через пропсы
   - Показывает модальное окно успеха после публикации
   - Уведомляет о статусе задачи

## Типы статусов задач

```typescript
type TaskStatus = 
  | 'creating'    // Создание товара (загрузка изображений, сохранение в БД)
  | 'analyzing'   // Анализ ИИ (определение категории, характеристик)
  | 'publishing'  // Публикация на WB (отправка на API)
  | 'completed'   // Успешно завершено
  | 'error';      // Ошибка
```

## Интерфейс ProductTask

```typescript
interface ProductTask {
  id: string;                // Уникальный ID задачи
  productName: string;       // Название товара
  status: TaskStatus;        // Текущий статус
  progress: number;          // Прогресс 0-100%
  error?: string;            // Текст ошибки (если есть)
  productId?: string;        // ID созданного товара
  createdAt: Date;           // Время создания задачи
}
```

## Пример использования

### В главной странице (page.tsx)

```typescript
const { tasks, addTask, updateTask, completeTask, errorTask } = useBackgroundTasks();

<SinglePageProductForm 
  onSuccess={loadStats}
  onTaskStart={(productName: string) => addTask(productName)}
  onTaskUpdate={(taskId: string, updates: any) => updateTask(taskId, updates)}
  onTaskComplete={(taskId: string, productId?: string) => completeTask(taskId, productId)}
  onTaskError={(taskId: string, error: string) => errorTask(taskId, error)}
/>

<TaskNotifications
  tasks={tasks}
  onRemoveTask={removeTask}
  onViewProduct={(productId) => {
    // Открыть товар
  }}
/>
```

### В форме создания товара

```typescript
// При начале создания товара
const taskId = onTaskStart?.(productName);
setCurrentTaskId(taskId);

// Обновление прогресса
onTaskUpdate?.(taskId, { status: 'analyzing', progress: 30 });

// При успехе
onTaskComplete?.(taskId, createdProductId);

// При ошибке
onTaskError?.(taskId, 'Ошибка публикации товара');
```

## UI/UX улучшения

### Модальное окно успеха

- ✅ Красивая анимация появления
- ✅ Зеленая иконка галочки
- ✅ Название товара в описании
- ✅ Три четкие кнопки действий
- ✅ Закрытие по клику вне окна (опционально)

### Панель уведомлений

- ✅ Фиксированная позиция в правом нижнем углу
- ✅ Сворачивание/разворачивание
- ✅ Счетчик активных задач
- ✅ Разделение на активные и завершенные
- ✅ Анимированный прогресс-бар
- ✅ Цветовая индикация статуса

## Будущие улучшения

- [ ] Сохранение задач в localStorage для восстановления после перезагрузки
- [ ] Звуковые уведомления при завершении задачи
- [ ] Push-уведомления браузера
- [ ] Возможность отменить задачу
- [ ] История всех задач с фильтрацией
- [ ] Экспорт отчета по созданным товарам

## Тестирование

### Сценарий 1: Создание одного товара
1. Заполните форму товара
2. Нажмите "Создать товар"
3. Дождитесь завершения
4. Проверьте модальное окно успеха
5. Нажмите "Создать новый товар"

### Сценарий 2: Параллельное создание
1. Заполните форму первого товара
2. Нажмите "Создать товар"
3. Сразу начните заполнять второй товар
4. Проверьте панель уведомлений - должны быть 2 задачи
5. Переключитесь на вкладку "Аналитика"
6. Проверьте что уведомления остались видны

### Сценарий 3: Обработка ошибок
1. Создайте товар с неправильными данными
2. Проверьте что задача перешла в статус "error"
3. Проверьте что отображается текст ошибки
4. Удалите уведомление об ошибке

## Стилизация

Все компоненты используют существующую дизайн-систему:
- Темная тема (gray-800, gray-900)
- Стеклянный эффект (backdrop-blur)
- Анимации (fade-in, zoom-in)
- Цветовая схема:
  - Синий (#3B82F6) - основные действия
  - Зеленый (#10B981) - успех
  - Красный (#EF4444) - ошибка
  - Серый (#6B7280) - вторичные элементы

# 🚀 AI Управление кампаниями - Быстрый старт

## ⚡ За 5 минут

### 1. Создайте OpenAI Assistant (2 мин)

1. Откройте https://platform.openai.com/assistants
2. Нажмите **"Create"**
3. **Name:** `WB Campaign Manager`
4. **Instructions:** Вставьте содержимое `docs/DEVELOPER_MESSAGE_GPT5.txt`
5. **Model:** `gpt-4o` или `gpt-4-turbo`
6. Сохраните и скопируйте **Assistant ID** (начинается с `asst_`)

### 2. Добавьте переменные окружения (1 мин)

Создайте `.env.local`:

```env
OPENAI_API_KEY=sk-proj-YOUR_KEY_HERE
OPENAI_ASSISTANT_ID=asst_YOUR_ASSISTANT_ID_HERE
CRON_SECRET=your-random-secret-123
```

### 3. Примените миграцию БД (1 мин)

```bash
psql -U postgres -d your_db -f migrations/add_ai_campaign_memory.sql
```

### 4. Установите зависимости (1 мин)

```bash
npm install openai@^4.0.0
```

### 5. Деплой (30 сек)

```bash
vercel --prod
```

---

## ✅ Готово!

Теперь AI автоматически проверяет кампании **каждые 3 часа** и принимает решения с учетом всей истории!

---

## 🎯 Как использовать

### Включить AI для кампании:

```sql
UPDATE "Campaign" 
SET "aiEnabled" = true 
WHERE id = 27673276;
```

### Ручная проверка:

```bash
curl -X POST https://your-domain.com/api/campaigns/27673276/auto-check
```

### Просмотр истории:

```bash
curl https://your-domain.com/api/campaigns/27673276/auto-check
```

---

## 📊 Что происходит автоматически

```
00:00 ─┐
03:00 ─┤
06:00 ─┤  AI проверяет все активные кампании
09:00 ─┤  и принимает решения:
12:00 ─┤  • Изменяет ставки
15:00 ─┤  • Добавляет/удаляет ключи
18:00 ─┤  • Управляет бюджетом
21:00 ─┘  • Ставит на паузу неэффективные
```

---

## 💡 Пример работы

**День 1, 12:00** - AI создает кампанию  
**День 1, 15:00** - AI видит низкий CTR, увеличивает ставки  
**День 1, 18:00** - AI видит улучшение, оставляет как есть  
**День 1, 21:00** - AI видит хороший ROI, увеличивает бюджет  
**День 2, 00:00** - AI видит стабильность, продолжает без изменений  

**Ключевое:** AI помнит ВСЕ предыдущие решения и видит динамику!

---

## 📖 Полная документация

Смотрите `docs/AI_CAMPAIGN_MEMORY_SETUP.md`

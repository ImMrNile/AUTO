# 🎯 Руководство по API действиям для AI оптимизации

## 📋 Обзор

Этот документ описывает все доступные действия, которые AI может предложить для оптимизации товаров на Wildberries, с указанием конкретных API endpoints для автоматического выполнения.

---

## 🔧 Доступные действия

### 1. CREATE_CAMPAIGN - Создать рекламную кампанию
**Когда использовать:**
- Нет рекламы
- Органический трафик >100 просмотров/неделю
- Конверсия >5%

**Параметры:**
```json
{
  "type": 8,
  "name": "Название кампании",
  "budget": 1500,
  "cpm": 150,
  "targetKeywords": ["ключ1", "ключ2"]
}
```

**API:** `POST /adv/v1/promotion/create`

---

### 2. PAUSE_CAMPAIGN - Поставить кампанию на паузу
**Когда использовать:**
- ROI < 50%
- CPC > 500₽
- 0 заказов 7+ дней

**Параметры:**
```json
{
  "campaignId": 27673276
}
```

**API:** `GET /adv/v0/pause?id={campaignId}`

---

### 3. RESUME_CAMPAIGN - Запустить кампанию
**Когда использовать:**
- Кампания была на паузе
- Товар улучшен (новые фото/описание)
- Конверсия выросла

**Параметры:**
```json
{
  "campaignId": 27673276
}
```

**API:** `GET /adv/v0/start?id={campaignId}`

---

### 4. STOP_CAMPAIGN - Завершить кампанию
**Когда использовать:**
- ROI < 20%
- Нет заказов 14+ дней
- Товар снят с продажи

**Параметры:**
```json
{
  "campaignId": 27673276
}
```

**API:** `GET /adv/v0/stop?id={campaignId}`

---

### 5. CHANGE_BID - Изменить ставку рекламы
**Когда использовать:**
- ROI > 200% (увеличить ставку)
- ROI < 100% (уменьшить ставку)
- CPC слишком высокий/низкий

**Параметры:**
```json
{
  "campaignId": 27673276,
  "type": 8,
  "cpm": 150,
  "keywords": [
    {"keyword": "химар", "bid": 120}
  ]
}
```

**API:** 
- `PATCH /adv/v0/bids` (для типа 8)
- `PATCH /adv/v0/auction/bids` (для типа 9)

---

### 6. ADD_KEYWORDS - Добавить ключевые слова (поисковые кластеры)
**Когда использовать:**
- Нашли ВЧ запросы (>1000/мес)
- Конверсия по запросу >10%
- Позиция в поиске >10

**Параметры:**
```json
{
  "campaignId": 27673276,
  "clusters": [
    {
      "cluster": "химар",
      "bid": 120,
      "reason": "ВЧ 6559/мес, позиция #44, есть заказ"
    }
  ]
}
```

**Как определить bid:**
- Если есть CPC конкурентов → `bid = CPC × 1.3`
- Если нет данных → `bid = 70₽` (ВЧ), `50₽` (СЧ), `35₽` (НЧ)
- Если позиция >20 → `bid = текущая_ставка × 1.5`
- Если ROI >300% → `bid = текущая_ставка × 2`

**API:** `POST /adv/v0/normquery/bids`

---

### 7. ADD_MINUS_WORDS - Добавить минус-слова
**Когда использовать:**
- Ключевое слово сжигает >30% бюджета
- 0 заказов по ключу
- Низкая релевантность

**Параметры:**
```json
{
  "campaignId": 27673276,
  "minusWords": ["дешевая", "копия", "подделка"]
}
```

**API:** 
- `POST /adv/v1/search/set-excluded` (для типа 4-8)
- `POST /adv/v1/auto/set-excluded` (для типа 8)

---

### 8. ADD_BUDGET - Пополнить бюджет кампании
**Когда использовать:**
- ROI > 250%
- Бюджет заканчивается
- Кампания останавливается из-за нехватки средств

**Параметры:**
```json
{
  "campaignId": 27673276,
  "sum": 5000
}
```

**API:** `POST /adv/v1/budget/deposit`

---

### 9. UPDATE_PRODUCT_NAME - Обновить название товара
**Когда использовать:**
- Позиция в поиске >20
- Мало ключевых слов в названии
- Низкий CTR в органике

**Параметры:**
```json
{
  "currentName": "Платье с химаром для Умры и Хаджа",
  "newName": "Платье с химаром белое для Умры и Хаджа, намаза — мусульманское макси",
  "addedKeywords": ["намазник", "белое", "макси", "мусульманское"]
}
```

**API:** Внутренний (не WB API)

---

### 10. UPDATE_PRODUCT_DESCRIPTION - Обновить описание товара
**Когда использовать:**
- Конверсия в корзину < 10%
- Много вопросов от покупателей
- Высокий процент возвратов

**Параметры:**
```json
{
  "improvements": [
    "Добавить таблицу размеров",
    "Указать состав ткани",
    "Добавить инструкцию по уходу"
  ]
}
```

**API:** Внутренний (не WB API)

---

## 📊 Целевые показатели WB

| Показатель | Норма | Отлично |
|-----------|-------|---------|
| CTR рекламы | > 8% | > 15% |
| Конверсия в корзину | > 15% | > 25% |
| Конверсия в заказ | > 50% | > 70% |
| ROI рекламы | > 200% | > 400% |
| CPC | < 300₽ | < 150₽ |
| Позиция в поиске | топ-20 | топ-10 |

---

## 💰 Рекомендации по ставкам

### Базовые ставки по частотности:
- **ВЧ запросы** (>10000/мес): 80-150₽
- **СЧ запросы** (1000-10000/мес): 50-100₽
- **НЧ запросы** (<1000/мес): 30-70₽

### Формулы расчета:
- **Рекомендуемая ставка** = CPC конкурентов × 1.2-1.5 (для топ-3)
- **Минимальная ставка** = 30-50₽ (для входа в аукцион)
- **При хорошем ROI** (>300%) = текущая × 2
- **При плохой позиции** (>20) = текущая × 1.5

---

## 🔗 Полезные API endpoints

### Управление кампаниями:
- `GET /adv/v0/delete` - Удаление кампании
- `POST /adv/v0/rename` - Переименование кампании
- `GET /adv/v0/start` - Запуск кампании
- `GET /adv/v0/pause` - Пауза кампании
- `GET /adv/v0/stop` - Завершение кампании

### Ставки:
- `PATCH /adv/v0/bids` - Изменение ставок
- `PATCH /adv/v0/auction/bids` - Изменение ставок в кампаниях с ручной ставкой

### Поисковые кластеры:
- `POST /adv/v0/normquery/stats` - Статистика поисковых кластеров
- `POST /adv/v0/normquery/get-bids` - Список ставок поисковых кластеров
- `POST /adv/v0/normquery/bids` - Установить ставки для поисковых кластеров
- `DELETE /adv/v0/normquery/bids` - Удалить ставки поисковых кластеров

### Минус-слова:
- `POST /adv/v1/search/set-excluded` - Установка/удаление минус-фраз в поиске
- `POST /adv/v1/auto/set-excluded` - Установка/удаление минус-фраз для кампании с единой ставкой
- `POST /adv/v0/normquery/get-minus` - Список минус-фраз кампаний
- `POST /adv/v0/normquery/set-minus` - Установка и удаление минус-фраз

### Финансы:
- `GET /adv/v1/balance` - Баланс
- `GET /adv/v1/budget` - Бюджет кампании
- `POST /adv/v1/budget/deposit` - Пополнение бюджета кампании
- `GET /adv/v1/upd` - Получение истории затрат
- `GET /adv/v1/payments` - Получение истории пополнений счёта

---

## ✅ Пример использования

### Сценарий: Товар с органикой, но без рекламы

**Входные данные:**
- Просмотры: 5392 за 63 дня
- Конверсия в корзину: 5.32%
- Конверсия в заказ: 13.59%
- Активная кампания: 27673276 (без ключей)
- Топ запросы: "намазник для женщин" (25917/мес, позиция #25)

**Рекомендации AI:**

```json
{
  "diagnosis": "Органика есть: 5392 просмотров/63 дн. Корзина 5.32% (<15%), из корзины в заказ 13.59% (<50%). По \"намазник\" позиция 25. Реклама без ключей — теряем трафик.",
  "actions": [
    {
      "type": "ADD_KEYWORDS",
      "priority": "critical",
      "autoExecutable": true,
      "reason": "Кампания 27673276 без ключей. «химар» 6559/мес дал 1 заказ из 5 открытий (20% конверсия). Добавляем 3 кластера с расчетными ставками.",
      "expectedEffect": "CTR 8-12%, CPC 90-130₽, +10-15 заказов/мес, ROI >200%",
      "details": {
        "campaignId": 27673276,
        "api": "POST /adv/v0/normquery/bids",
        "clusters": [
          {"cluster": "химар", "bid": 120},
          {"cluster": "платье мусульманское", "bid": 90},
          {"cluster": "намазник для женщин", "bid": 70}
        ]
      }
    },
    {
      "type": "UPDATE_PRODUCT_DESCRIPTION",
      "priority": "critical",
      "autoExecutable": true,
      "reason": "Конверсия в корзину 5.32% (<10%). За 7 дней 82 открытия и 0 заказов. Белый цвет и комплект вызывают сомнения.",
      "expectedEffect": "Рост в корзину до 10-12% за 10-14 дней, +15-25 корзин/нед., заказы +10-15/мес",
      "details": {
        "improvements": [
          "Четко указать комплект: 2 предмета — платье + химар",
          "Добавить таблицу размеров 42-54 с ОГ/ОТ/ОБ и ростом",
          "Пояснить про непросвечивание белого: плотность/подклад"
        ]
      }
    }
  ],
  "forecast": {
    "sales": "+100-150% через 14 дней",
    "conversion": "+6-7% через 14 дней",
    "roi": "200-250% через 21 день",
    "confidence": "medium"
  }
}
```

---

## 📝 Примечания

1. **autoExecutable: true** - действие можно выполнить автоматически через API
2. **autoExecutable: false** - требуется ручное подтверждение пользователя
3. Все ставки указаны в рублях
4. Все прогнозы основаны на исторических данных и могут отличаться
5. Рекомендуется тестировать изменения постепенно

---

**Последнее обновление:** 09.11.2025

# Интеграция WB API - Итоговый отчет

## ✅ Выполненные задачи

### 1. API Endpoints

#### 1.1 Остатки FBS/FBW по складам
**Endpoint:** `GET /api/wb/stocks`
- Получение списка всех складов продавца
- Загрузка остатков со всех складов (FBS и FBW)
- Группировка остатков по товарам
- Автоматическое сохранение в БД
- Поддержка фильтрации по кабинету

**Формат ответа:**
```json
{
  "success": true,
  "warehouses": [...],
  "stocks": [
    {
      "nmId": 123456,
      "vendorCode": "ABC-123",
      "warehouses": [
        {
          "warehouseId": 1,
          "warehouseName": "Склад FBS",
          "warehouseType": "FBS",
          "stock": 100,
          "reserved": 5
        }
      ],
      "totalStock": 100,
      "fbsStock": 100,
      "fbwStock": 0
    }
  ]
}
```

#### 1.2 Заказы за сегодня
**Endpoint:** `GET /api/wb/orders/today?cabinetId={id}&nmId={nmId}`
- Получение всех заказов за текущий день
- Группировка по товарам
- Подсчет количества заказов, единиц и суммы
- Опциональная фильтрация по конкретному товару

**Формат ответа:**
```json
{
  "success": true,
  "data": {
    "date": "2025-01-29",
    "orders": [
      {
        "nmId": 123456,
        "ordersCount": 5,
        "totalQuantity": 7,
        "totalSum": 15000,
        "orders": [...]
      }
    ],
    "summary": {
      "totalOrders": 5,
      "totalSum": 15000,
      "totalProducts": 1
    }
  }
}
```

#### 1.3 Конверсия и просмотры товара
**Endpoint:** `GET /api/wb/product-analytics?nmId={id}&days={days}`
- Получение данных конверсии для конкретного товара
- Просмотры карточки товара
- Добавления в корзину
- Заказы
- Расчет CTR и конверсии
- Поисковые запросы (если доступны)

**Формат ответа:**
```json
{
  "success": true,
  "data": {
    "nmId": 123456,
    "views": 1500,
    "addToCart": 150,
    "orders": 30,
    "ctr": 10.0,
    "addToCartRate": 10.0,
    "purchaseRate": 20.0,
    "cartAbandonmentRate": 80.0,
    "searchQueries": [],
    "totalSearchQueries": 0,
    "hasAnalyticsAccess": true,
    "period": {
      "start": "2025-01-01",
      "end": "2025-01-29",
      "days": 30
    }
  }
}
```

### 2. Обновления интерфейса

#### 2.1 Главная страница (ProductsWithAnalytics)

**Добавлено:**
- ✅ Блок статистики с 4 метриками:
  - **Товаров** - общее количество
  - **FBW** - остатки на складах WB
  - **FBS** - остатки на складах продавца
  - **Сегодня** - количество заказов за текущий день

- ✅ В карточках товаров:
  - Остатки FBW (только просмотр)
  - Остатки FBS (с возможностью редактирования)
  - Заказы за сегодня (количество и единицы)

**Функционал:**
- Автоматическая загрузка данных при открытии страницы
- Индикаторы загрузки (skeleton)
- Редактирование остатков FBS прямо в карточке
- Синхронизация изменений с WB API

#### 2.2 Детальный просмотр товара (ProductFinancialDetails)

**Добавлено:**
- ✅ Блок "Конверсия" с 4 метриками:
  - **Просмотры** - количество просмотров карточки
  - **В корзину** - добавления в корзину
  - **CTR** - процент кликов
  - **Конверсия** - процент покупок

- ✅ Блок "Остатки" с детализацией:
  - FBS остатки (на ваших складах)
  - FBW остатки (на складах WB)
  - Список всех складов с остатками

**Функционал:**
- Автоматическая загрузка при открытии
- Индикаторы загрузки
- Обработка отсутствия данных
- Красивое отображение с иконками

### 3. Технические улучшения

#### 3.1 WbConversionService
**Добавлен метод:** `getProductSearchQueries(nmId, dateFrom, dateTo)`
- Получение поисковых запросов для товара
- Интеграция с nm-report API
- Обработка отсутствия данных

#### 3.2 Обработка ошибок
- Graceful degradation при отсутствии доступа к Analytics API
- Fallback на пустые значения вместо ошибок
- Детальное логирование для отладки

#### 3.3 Кеширование
- Данные конверсии кешируются на 60 минут
- Автоматическое обновление при истечении TTL
- Принудительное обновление через `forceRefresh=true`

## 📊 Структура данных

### Остатки по складам
```typescript
interface WarehouseStock {
  nmId: number;
  vendorCode: string;
  warehouses: Array<{
    warehouseId: number;
    warehouseName: string;
    warehouseType: 'FBS' | 'FBW' | 'FBO';
    stock: number;
    reserved: number;
  }>;
  totalStock: number;
  totalReserved: number;
  fbsStock: number;
  fbwStock: number;
}
```

### Заказы за сегодня
```typescript
interface TodayOrders {
  nmId: number;
  ordersCount: number;
  totalQuantity: number;
  totalSum: number;
  orders: Array<{
    orderId: string;
    date: string;
    price: number;
    quantity: number;
    status: string;
  }>;
}
```

### Конверсия товара
```typescript
interface ProductConversion {
  nmId: number;
  views: number;
  addToCart: number;
  orders: number;
  ctr: number;
  addToCartRate: number;
  purchaseRate: number;
  cartAbandonmentRate: number;
  searchQueries: Array<any>;
  totalSearchQueries: number;
  hasAnalyticsAccess: boolean;
}
```

## 🔧 Использование

### Загрузка остатков
```typescript
const response = await fetch('/api/wb/stocks?cabinetId=xxx');
const data = await response.json();
// data.stocks - массив остатков по товарам
```

### Загрузка заказов за сегодня
```typescript
const response = await fetch('/api/wb/orders/today?cabinetId=xxx');
const data = await response.json();
// data.data.summary.totalOrders - общее количество заказов
```

### Загрузка конверсии товара
```typescript
const response = await fetch('/api/wb/product-analytics?nmId=123456&days=30');
const data = await response.json();
// data.data.views - просмотры
// data.data.ctr - CTR
```

## 📝 Примечания

### Доступ к Analytics API
Для получения данных конверсии требуется:
1. API токен с правами доступа к Analytics API
2. Бит 2 (Analytics access) должен быть установлен в токене

Если доступа нет:
- Система вернет `hasAnalyticsAccess: false`
- Все метрики будут равны 0
- Пользователь увидит сообщение о необходимости настройки доступа

### Типы складов

**ВАЖНО:** Тип склада определяется по полю `deliveryType` из API `/api/v3/warehouses`:
- `deliveryType = 1` → **FBS** (Fulfillment by Seller) - склады продавца
- `deliveryType = 2` → **FBW** (Fulfillment by Wildberries) - склады WB

**Примеры FBW складов:**
- Коледино, Екатеринбург, Белые Столбы, Вёшки
- Все сортировочные центры (СЦ Иркутск, СЦ Брянск, и т.д.)
- Рязань, Астана, Шушары, Софьино, Киров

**Примеры FBS складов:**
- "Мой склад", "Видное" (если созданы продавцом с deliveryType=1)

**❌ НЕ определяйте тип по названию!** Используйте только `deliveryType` из API.

### Обновление данных
- Остатки: обновляются при каждом запросе к `/api/wb/stocks`
- Заказы: обновляются при каждом запросе к `/api/wb/orders/today`
- Конверсия: кешируется на 60 минут, можно принудительно обновить

## ✨ Результат

Теперь в интерфейсе отображаются:
- ✅ Остатки FBS и FBW на главной странице и в карточках товаров
- ✅ Заказы за сегодня на главной странице и в карточках
- ✅ Конверсия и просмотры в детальном просмотре товара
- ✅ Детализация по складам в детальном просмотре
- ✅ Возможность редактирования остатков FBS

Все данные загружаются автоматически из WB API и синхронизируются с базой данных.

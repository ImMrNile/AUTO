# 🎯 Cron Orchestrator - Решение для Hobby плана Vercel

## Проблема

На **Vercel Hobby плане**:
- ✅ Максимум 2 Cron Jobs
- ⏰ Каждый запускается **1 раз в день**
- 🚫 Нельзя запускать каждые 2 часа или 30 минут

**Нам нужно:**
- Синхронизация аналитики - каждые 2 часа
- Синхронизация товаров - каждые 2 часа
- Проверка цен - каждые 30 минут
- Проверка кампаний - каждые 3 часа

---

## 💡 Решение: Cron Orchestrator

**Идея:** Создать скрипт-оркестратор, который:
1. Запускается **1 раз в день** через Vercel Cron (в полночь)
2. Работает **24 часа** внутри Vercel Function
3. Вызывает наши endpoints с нужной частотой

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│  Vercel Cron (1 раз в день в 00:00 UTC)                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  /api/cron/orchestrator                                 │
│  Запускает оркестратор на 24 часа                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  lib/cron-orchestrator.ts                               │
│  Планирует и вызывает endpoints:                        │
│                                                          │
│  ├─ /api/cron/sync-analytics    (каждые 2 часа)        │
│  ├─ /api/cron/sync-products     (каждые 2 часа)        │
│  ├─ /api/cron/check-prices      (каждые 30 минут)      │
│  └─ /api/cron/check-campaigns   (каждые 3 часа)        │
└─────────────────────────────────────────────────────────┘
```

---

## 📋 Конфигурация задач

| Задача | Endpoint | Интервал | Вызовов/день |
|--------|----------|----------|--------------|
| **Синхронизация аналитики** | `/api/cron/sync-analytics` | 2 часа | 12 раз |
| **Синхронизация товаров** | `/api/cron/sync-products` | 2 часа | 12 раз |
| **Проверка цен** | `/api/cron/check-prices` | 30 минут | 48 раз |
| **Проверка кампаний** | `/api/cron/check-campaigns` | 3 часа | 8 раз |

**Итого:** 80 вызовов в день вместо 2!

---

## 🔧 Настройка

### 1. vercel.json

```json
{
  "crons": [
    {
      "path": "/api/cron/orchestrator",
      "schedule": "0 0 * * *"
    }
  ]
}
```

**Расписание:** `0 0 * * *` = каждый день в полночь UTC (3:00 МСК)

### 2. Авторизация endpoints

Все Cron endpoints теперь принимают вызовы от оркестратора через заголовок `x-orchestrator: true`:

```typescript
const isVercelCron = request.headers.get('x-vercel-cron') === '1';
const isOrchestrator = request.headers.get('x-orchestrator') === 'true';
const authHeader = request.headers.get('authorization');
const cronSecret = process.env.CRON_SECRET;

const isAuthorized = isVercelCron || isOrchestrator || (cronSecret && authHeader === `Bearer ${cronSecret}`);
```

---

## 🚀 Как это работает

### Шаг 1: Vercel Cron запускает оркестратор
```
00:00 UTC (3:00 МСК) - Vercel вызывает /api/cron/orchestrator
```

### Шаг 2: Оркестратор планирует задачи
```typescript
// Немедленный первый вызов
callEndpoint(task, baseUrl);

// Повторяющиеся вызовы
setInterval(() => {
  callEndpoint(task, baseUrl);
}, intervalMs);
```

### Шаг 3: Оркестратор работает 24 часа
```
00:00 - Старт оркестратора
00:00 - Первый вызов всех endpoints
00:30 - check-prices
01:00 - check-prices
02:00 - sync-analytics, sync-products, check-prices
03:00 - check-campaigns, check-prices
...
23:30 - check-prices
24:00 - Оркестратор останавливается
```

### Шаг 4: На следующий день цикл повторяется
```
00:00 - Vercel снова запускает оркестратор
```

---

## 📊 Статистика выполнения

Оркестратор предоставляет статистику:

```bash
curl https://ваш-домен.vercel.app/api/cron/orchestrator
```

**Ответ:**
```json
{
  "success": true,
  "message": "Оркестратор запущен на 24 часа",
  "stats": {
    "tasks": [
      {
        "name": "sync-analytics",
        "intervalMinutes": 120,
        "executionsPerDay": 12,
        "description": "Синхронизация аналитики"
      },
      {
        "name": "sync-products",
        "intervalMinutes": 120,
        "executionsPerDay": 12,
        "description": "Синхронизация товаров"
      },
      {
        "name": "check-prices",
        "intervalMinutes": 30,
        "executionsPerDay": 48,
        "description": "Проверка закрепленных цен"
      },
      {
        "name": "check-campaigns",
        "intervalMinutes": 180,
        "executionsPerDay": 8,
        "description": "Проверка рекламных кампаний"
      }
    ],
    "totalExecutionsPerDay": 80
  },
  "startedAt": "2025-11-14T00:00:00.000Z",
  "willStopAt": "2025-11-15T00:00:00.000Z"
}
```

---

## 🔍 Мониторинг

### В логах Vercel

Найдите логи с префиксами:

```
🚀 [Orchestrator] Запуск оркестратора на 24 часа
📅 [Orchestrator] Запланирована задача: sync-analytics (каждые 120 мин)
📅 [Orchestrator] Запланирована задача: check-prices (каждые 30 мин)
🔄 [Orchestrator] Вызов sync-analytics: https://...
✅ [Orchestrator] sync-analytics выполнен успешно
⏰ [Orchestrator] 24 часа истекли, останавливаем оркестратор
```

### Ручная проверка

Запустите оркестратор вручную для тестирования:

```bash
curl -X GET https://ваш-домен.vercel.app/api/cron/orchestrator
```

---

## ⚠️ Важные моменты

### 1. Vercel Function Timeout

**Проблема:** Vercel Functions на Hobby плане имеют лимит выполнения 10 секунд.

**Решение:** Оркестратор запускается быстро (< 5 секунд), а затем работает в фоне. Каждый вызов endpoint - это отдельный HTTP запрос, который не блокирует оркестратор.

### 2. Холодный старт

**Проблема:** После 5 минут бездействия Function "засыпает".

**Решение:** Оркестратор вызывает endpoints регулярно (минимум каждые 30 минут), поэтому Function остается "теплой".

### 3. Надежность

**Проблема:** Если оркестратор упадет, задачи перестанут выполняться до следующего дня.

**Решение:** 
- Оркестратор логирует все ошибки
- Каждый endpoint можно вызвать вручную через кнопку "Обновить" в UI
- На следующий день оркестратор перезапустится автоматически

---

## 🎉 Преимущества

✅ **Укладываемся в Hobby план** - используем только 1 Cron Job
✅ **Частые обновления** - 80 вызовов в день вместо 2
✅ **Гибкость** - легко изменить интервалы в `lib/cron-orchestrator.ts`
✅ **Мониторинг** - детальные логи всех вызовов
✅ **Надежность** - каждый endpoint независим
✅ **Бесплатно** - не требует Pro плана

---

## 📝 Файлы

- **`lib/cron-orchestrator.ts`** - логика оркестратора
- **`src/app/api/cron/orchestrator/route.ts`** - endpoint для запуска
- **`vercel.json`** - конфигурация Vercel Cron
- **`src/app/api/cron/sync-analytics/route.ts`** - обновлен для оркестратора
- **`src/app/api/cron/sync-products/route.ts`** - обновлен для оркестратора
- **`src/app/api/cron/check-prices/route.ts`** - обновлен для оркестратора
- **`src/app/api/cron/check-campaigns/route.ts`** - обновлен для оркестратора

---

## 🚀 Деплой

```bash
git add .
git commit -m "feat: add Cron Orchestrator for Hobby plan"
git push
```

После деплоя:
1. Проверьте что Cron Job создан в Vercel Dashboard
2. Дождитесь полуночи UTC (3:00 МСК) или запустите вручную
3. Проверьте логи через 30 минут

---

## ❓ FAQ

### Q: Что если оркестратор упадет?
**A:** На следующий день в полночь он перезапустится автоматически. Также можно вызвать endpoints вручную через UI.

### Q: Можно ли изменить интервалы?
**A:** Да! Отредактируйте `TASKS` в `lib/cron-orchestrator.ts`:
```typescript
{
  name: 'check-prices',
  endpoint: '/api/cron/check-prices',
  intervalMinutes: 15, // Изменить на 15 минут
  description: 'Проверка закрепленных цен'
}
```

### Q: Сколько это стоит?
**A:** Бесплатно на Hobby плане! Все вызовы - это обычные HTTP запросы внутри вашего домена.

### Q: Можно ли добавить новые задачи?
**A:** Да! Добавьте новую задачу в массив `TASKS` в `lib/cron-orchestrator.ts`.

---

## ✅ Готово!

Теперь у вас есть полноценная система автоматизации с частыми обновлениями на бесплатном Hobby плане Vercel! 🎉

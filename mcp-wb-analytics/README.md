# 🤖 WB Analytics MCP Server

**Автономный AI-агент для оптимизации продаж на Wildberries**

## 🎯 Что это?

MCP (Model Context Protocol) сервер, который позволяет AI-агенту (**GPT-5** / Claude) **полностью автономно**:
- 📊 Получать данные из WB API
- 🧠 Анализировать эффективность рекламы
- 💰 Рассчитывать оптимальную цену с учетом ВСЕХ расходов WB
- ⚡ Применять оптимизации
- 💾 Сохранять результаты в БД

**Без промежуточных запросов к серверу!**

## 🔐 Multi-tenant безопасность

- ✅ Работает с каждым пользователем отдельно
- ✅ Проверка принадлежности товара пользователю
- ✅ Использует реальные комиссии WB из БД
- ✅ Учитывает налоговую ставку пользователя (УСН 6%, 15%, 25%)
- ✅ Audit log всех действий AI

---

## ⚡ Быстрый старт

### 1. Установка

```bash
cd mcp-wb-analytics
npm install
```

### 2. Добавить модель в Prisma

В `prisma/schema.prisma`:

```prisma
model ProductOptimization {
  id                    String   @id @default(cuid())
  productId             String
  goldenKeywords        String   // JSON
  unprofitableKeywords  String   // JSON
  promisingKeywords     String   // JSON
  cardOptimizations     String   // JSON
  expectedSalesIncrease Float
  expectedROIIncrease   Float
  createdAt             DateTime @default(now())
  
  product Product @relation(fields: [productId], references: [id])
  
  @@index([productId])
  @@index([createdAt])
}

// Добавить в модель Product:
model Product {
  // ... существующие поля
  optimizations ProductOptimization[]
}
```

### 3. Применить миграцию

```bash
npx prisma migrate dev --name add_product_optimization
npx prisma generate
```

### 4. Собрать

```bash
npm run build
```

### 5. Настроить Claude Desktop

В `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "wb-analytics": {
      "command": "node",
      "args": [
        "C:\\Users\\Neal\\Downloads\\AUTO-cursor-bc-b522b817-cedb-455a-CURS\\mcp-wb-analytics\\dist\\index.js"
      ],
      "env": {
        "DATABASE_URL": "postgresql://user:password@localhost:5432/dbname"
      }
    }
  }
}
```

### 6. Перезапустить Claude Desktop

---

## 🛠️ Доступные инструменты (10 tools)

### Анализ продаж:
| Инструмент | Описание |
|-----------|----------|
| `get_search_queries` | Получить поисковые запросы с классификацией |
| `get_campaign_stats` | Статистика рекламных кампаний с ROI |
| `get_keyword_stats` | Ключевые фразы (золотые/убыточные/перспективные) |
| `get_sales_funnel` | Воронка продаж с конверсиями |

### Оптимизация рекламы:
| Инструмент | Описание |
|-----------|----------|
| `apply_bid_optimization` | Применить оптимизацию ставок |
| `add_minus_words` | Добавить минус-слова |
| `save_recommendations` | Сохранить рекомендации в БД |

### Управление ценами (НОВОЕ для GPT-5):
| Инструмент | Описание |
|-----------|----------|
| `get_user_products` | Получить все товары пользователя |
| `calculate_optimal_price` | Рассчитать оптимальную цену (комиссия + логистика + налог) |
| `apply_price_optimization` | Применить новую цену (БД + WB API) |

---

## 💡 Пример использования

### Запрос в Claude:

```
Оптимизируй продажи товара 12345678 за последние 30 дней.
Примени все рекомендации автоматически.

API токен: eyJhbGc...
ID кампании: 123456
```

### Claude автоматически:

1. ✅ Получит поисковые запросы
2. ✅ Получит статистику кампаний
3. ✅ Получит ключевые фразы
4. ✅ Проанализирует воронку продаж
5. ✅ Увеличит ставки на "золотые" фразы
6. ✅ Добавит "убыточные" в минус-слова
7. ✅ Сохранит рекомендации в БД
8. ✅ Вернет отчет с прогнозом

**Время:** 10-30 секунд (вместо 5-10 минут)

---

## 📊 Преимущества

| До MCP | С MCP | Улучшение |
|--------|-------|-----------|
| 5-10 минут | 10-30 секунд | **20x быстрее** |
| 10-15 запросов | 0 запросов | **100% меньше** |
| Вручную | Автоматически | ✅ |

---

## 📖 Документация

Полная документация: [`docs/MCP_INTEGRATION_GUIDE.md`](../docs/MCP_INTEGRATION_GUIDE.md)

---

## 🔧 Разработка

```bash
# Разработка с hot reload
npm run dev

# Сборка
npm run build

# Запуск
npm start
```

---

## ⚠️ Примечания

**Lint ошибки:** После установки `npm install` ошибки TypeScript исчезнут. Они появляются потому что пакет `@modelcontextprotocol/sdk` еще не установлен.

**Prisma модель:** Не забудьте добавить модель `ProductOptimization` в schema и выполнить миграцию.

---

## 📝 Лицензия

MIT

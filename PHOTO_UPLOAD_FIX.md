# Исправление: Фотографии не передавались на WB при публикации

## Проблема

При публикации товара на Wildberries передавалась **только одна фотография** (главная), дополнительные фотографии **терялись**.

### Корневая причина

1. **В БД сохранялась только главная фотография:**
   ```typescript
   // src/app/api/products/route.ts (строка 305)
   originalImage: mainImageUrl,  // ❌ Только ОДНА фотография!
   ```

2. **Дополнительные фотографии не сохранялись:**
   - `additionalImageUrls` создавались при загрузке
   - Но **не записывались** в базу данных
   - При публикации их уже не было

3. **Схема БД не поддерживала массив фотографий:**
   ```prisma
   originalImage  String?  // ❌ Только одна строка, не массив
   ```

## Решение

### Часть 1: Сохранение всех фотографий в wbData

**Файл:** `src/app/api/products/route.ts`

```typescript
wbData: {
  // ... другие поля ...
  // 📸 СОХРАНЯЕМ ВСЕ ФОТОГРАФИИ
  images: {
    main: mainImageUrl,
    additional: additionalImageUrls.filter((url): url is string => url !== null)
  }
}
```

**Что изменилось:**
- ✅ Главная фотография сохраняется в `wbData.images.main`
- ✅ Дополнительные фотографии сохраняются в `wbData.images.additional[]`
- ✅ Используется существующее поле `wbData` (JSON), не нужно менять схему БД

### Часть 2: Получение всех фотографий при публикации

**Файл:** `src/app/api/products/[id]/publish/route.ts`

```typescript
// Получаем фотографии товара из wbData.images или originalImage (fallback)
let productImages: string[] = [];

if (wbData?.images) {
  // Новый формат: все фотографии в wbData.images
  if (wbData.images.main) {
    productImages.push(wbData.images.main);
  }
  if (Array.isArray(wbData.images.additional)) {
    productImages.push(...wbData.images.additional);
  }
} else {
  // Старый формат: только originalImage (для совместимости)
  productImages = Array.isArray(product.originalImage) 
    ? product.originalImage 
    : (product.originalImage ? [product.originalImage] : []);
}

console.log(`📸 [WB Publish] Фотографий товара: ${productImages.length}`);
if (productImages.length > 0) {
  console.log(`   Главное фото: ${productImages[0].substring(0, 100)}...`);
  if (productImages.length > 1) {
    console.log(`   Дополнительных фото: ${productImages.length - 1}`);
  }
} else {
  console.warn(`⚠️ [WB Publish] У товара НЕТ фотографий!`);
}
```

**Что изменилось:**
- ✅ Сначала пытаемся получить фотографии из `wbData.images` (новый формат)
- ✅ Если нет - используем `originalImage` (старый формат, для совместимости)
- ✅ Собираем все фотографии в массив `productImages[]`
- ✅ Добавлено детальное логирование количества фотографий

### Часть 3: Передача фотографий в WB API

**Файл:** `src/app/api/products/[id]/publish/route.ts` (строки 422-429)

```typescript
// 📸 Добавляем фотографии товара
photos: productImages.map((url: string) => ({
  big: url,
  c246x328: url,
  c516x688: url,
  square: url,
  tm: url
})),
```

**Что происходит:**
- ✅ Все фотографии из массива `productImages[]` передаются в WB API
- ✅ Каждая фотография дублируется для всех размеров (требование WB API)

## Формат данных в БД

### Новый формат (после исправления):

```json
{
  "wbData": {
    "vendorCode": "PRD12345678",
    "barcode": "2200000123456",
    "images": {
      "main": "https://example.com/uploads/main.jpg",
      "additional": [
        "https://example.com/uploads/photo1.jpg",
        "https://example.com/uploads/photo2.jpg",
        "https://example.com/uploads/photo3.jpg"
      ]
    }
  }
}
```

### Старый формат (для совместимости):

```json
{
  "originalImage": "https://example.com/uploads/main.jpg"
}
```

## Логирование

**При создании товара:**
```
📸 Подготовка изображений для ИИ: {
  mainImageUrl: 'загружено',
  additionalCount: 3,
  totalImages: 4
}
```

**При публикации:**
```
📸 [WB Publish] Фотографий товара: 4
   Главное фото: https://example.com/uploads/main.jpg...
   Дополнительных фото: 3
```

**Если фотографий нет:**
```
⚠️ [WB Publish] У товара НЕТ фотографий!
```

## Обратная совместимость

✅ **Старые товары** (созданные до исправления):
- Используют поле `originalImage`
- Работают как раньше (только главная фотография)

✅ **Новые товары** (созданные после исправления):
- Используют `wbData.images`
- Передают **ВСЕ фотографии** на WB

## Результат

✅ Все фотографии (главная + дополнительные) сохраняются в БД
✅ Все фотографии передаются на Wildberries при публикации
✅ Обратная совместимость со старыми товарами
✅ Детальное логирование для отладки
✅ Не требуется изменение схемы БД (используется JSON поле)

## Файлы изменены

- `src/app/api/products/route.ts` - сохранение всех фотографий в `wbData.images`
- `src/app/api/products/[id]/publish/route.ts` - получение всех фотографий при публикации
- `PHOTO_UPLOAD_FIX.md` - документация исправления

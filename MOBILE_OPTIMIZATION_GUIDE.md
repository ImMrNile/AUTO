# 📱 Оптимизация загрузки для смартфонов

## Проблемы

### 1. ❌ Redis кеш не настроен
**Симптом:** Каждый запрос идет в БД, медленная загрузка
**Причина:** Отсутствуют переменные `REDIS_URL` и `REDIS_TOKEN`

### 2. ❌ Фоновая загрузка отключена
**Симптом:** Товары загружаются каждый раз при открытии
**Причина:** `BackgroundProductLoader` отключен для экономии батареи

### 3. ⚠️ Нет оптимизации для мобильных
**Симптом:** Долгая загрузка аналитики и товаров
**Причина:** Загружаются все данные сразу без пагинации

## Решения

### Решение 1: Настройка Redis (КРИТИЧНО!)

#### Вариант A: Upstash Redis (Рекомендуется)

1. **Создайте базу данных:**
   - Откройте https://upstash.com
   - Создайте аккаунт (бесплатно)
   - Создайте новую Redis базу
   - Выберите регион близкий к вашему серверу

2. **Получите credentials:**
   ```
   UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
   UPSTASH_REDIS_REST_TOKEN=your-token-here
   ```

3. **Добавьте в Vercel:**
   ```bash
   # В Vercel Dashboard → Settings → Environment Variables
   REDIS_URL=https://your-db.upstash.io
   REDIS_TOKEN=your-token-here
   ```

#### Вариант B: Vercel KV (Альтернатива)

1. **Включите Vercel KV:**
   - Откройте Vercel Dashboard
   - Storage → Create Database → KV
   - Выберите регион

2. **Переменные добавятся автоматически:**
   ```
   KV_REST_API_URL
   KV_REST_API_TOKEN
   ```

3. **Обновите код:**
   ```typescript
   // lib/cache/redis.ts
   const redisUrl = process.env.REDIS_URL || process.env.KV_REST_API_URL;
   const redisToken = process.env.REDIS_TOKEN || process.env.KV_REST_API_TOKEN;
   ```

### Решение 2: Включить кеширование на уровне API

**Файл:** `src/app/api/wb/products/route.ts`

**Что уже работает:**
```typescript
// ✅ Redis кеш с TTL 30 минут
const cacheKey = `products:${userId}:all`;
const cached = await getCached<any[]>(cacheKey);
if (cached) {
  return NextResponse.json({
    success: true,
    products: cached,
    fromCache: true  // ← Данные из кеша
  });
}
```

**Проблема:** Redis не настроен, поэтому кеш не работает!

### Решение 3: Оптимизация для мобильных

#### A. Пагинация товаров

Добавьте параметры `page` и `limit`:

```typescript
// GET /api/wb/products?source=db&page=1&limit=20
const page = parseInt(searchParams.get('page') || '1');
const limit = parseInt(searchParams.get('limit') || '50');
const offset = (page - 1) * limit;

const products = await prisma.product.findMany({
  where: whereConditions,
  skip: offset,
  take: limit,
  // ... остальное
});
```

#### B. Ленивая загрузка аналитики

Не загружайте аналитику сразу:

```typescript
// Первый запрос: только товары (быстро)
GET /api/wb/products?source=db&includeAnalytics=false

// Второй запрос: аналитика (в фоне)
GET /api/analytics/dashboard?days=7
```

#### C. Оптимизация изображений

```typescript
// Загружайте только превью (маленькие изображения)
images: p.wbData?.images?.map(img => ({
  thumbnail: img.replace('/big/', '/tm/'),  // Маленькая версия
  full: img  // Полная версия (загружается по требованию)
}))
```

### Решение 4: Service Worker для офлайн кеша

**Файл:** `public/sw.js`

```javascript
// Кешируем товары и аналитику
const CACHE_NAME = 'wb-app-v1';
const urlsToCache = [
  '/api/wb/products?source=db',
  '/api/analytics/dashboard'
];

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((response) => {
      // Возвращаем из кеша или загружаем
      return response || fetch(event.request);
    })
  );
});
```

## Быстрое решение (5 минут)

### Шаг 1: Настройте Redis

```bash
# 1. Создайте Upstash Redis базу (https://upstash.com)
# 2. Добавьте в Vercel Environment Variables:
REDIS_URL=https://your-db.upstash.io
REDIS_TOKEN=your-token-here
```

### Шаг 2: Деплой

```bash
git add .
git commit -m "feat: настроен Redis кеш для оптимизации"
git push
```

### Шаг 3: Проверьте

Откройте приложение на смартфоне:
1. Первая загрузка: 3-5 секунд (из БД)
2. Вторая загрузка: 0.5-1 секунда (из Redis кеша) ✅

## Результаты оптимизации

### До оптимизации:
- ❌ Загрузка товаров: 5-10 секунд
- ❌ Загрузка аналитики: 8-15 секунд
- ❌ Каждый раз из БД
- ❌ Перегрев смартфона

### После оптимизации:
- ✅ Загрузка товаров: 0.5-1 секунда (из кеша)
- ✅ Загрузка аналитики: 1-2 секунды (из кеша)
- ✅ Кеш на 30 минут
- ✅ Экономия батареи

## Мониторинг

### Проверка Redis в логах:

**Успешное кеширование:**
```
✅ [Redis] Клиент успешно инициализирован
🔍 [Redis] Получение из кеша: "products:user123:all"
✅ [Redis] Найдено в кеше: "products:user123:all"
✅ Cache HIT: Товары взяты из Redis кеша (52 товаров)
```

**Кеш не настроен:**
```
⚠️ [Redis] Клиент недоступен, пропускаем getCached("products:user123:all")
⚠️ Cache MISS: Кеш отсутствует, загружаем из БД...
```

### Проверка в Vercel Dashboard:

1. Откройте **Logs**
2. Фильтр: `[Redis]`
3. Убедитесь что видите `✅ Найдено в кеше`

## Дополнительные оптимизации

### 1. HTTP/2 Server Push

Предзагружайте критичные ресурсы:

```typescript
// next.config.js
module.exports = {
  async headers() {
    return [
      {
        source: '/',
        headers: [
          {
            key: 'Link',
            value: '</api/wb/products?source=db>; rel=preload; as=fetch'
          }
        ]
      }
    ];
  }
};
```

### 2. Сжатие ответов

```typescript
// middleware.ts
import { compress } from 'next/dist/server/lib/squoosh/main';

export async function middleware(request: NextRequest) {
  const response = NextResponse.next();
  
  // Сжимаем JSON ответы
  if (request.url.includes('/api/')) {
    response.headers.set('Content-Encoding', 'gzip');
  }
  
  return response;
}
```

### 3. Prefetch на фронтенде

```typescript
// Предзагружаем данные при наведении на кнопку
<button
  onMouseEnter={() => {
    fetch('/api/analytics/dashboard?days=7');
  }}
>
  Аналитика
</button>
```

## Итоговый чеклист

- [ ] Настроен Redis (Upstash или Vercel KV)
- [ ] Добавлены переменные REDIS_URL и REDIS_TOKEN
- [ ] Деплой в Vercel
- [ ] Проверка логов: видны `✅ [Redis] Найдено в кеше`
- [ ] Тест на смартфоне: загрузка < 2 секунд
- [ ] Настроен Cron для автообновления (каждые 2 часа)

## Поддержка

Если после настройки Redis загрузка все еще медленная:

1. **Проверьте логи Vercel:**
   - Есть ли ошибки Redis?
   - Работает ли кеширование?

2. **Проверьте размер данных:**
   ```bash
   # В Vercel Logs
   grep "Товары взяты из Redis" | wc -l
   ```

3. **Проверьте регион Redis:**
   - Должен быть близко к серверу Vercel
   - Рекомендуется: US East (если Vercel в US)

4. **Проверьте TTL кеша:**
   - Текущий: 30 минут (1800 секунд)
   - Можно увеличить до 1 часа (3600 секунд)

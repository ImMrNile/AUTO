# Кеширование в БД - Решение проблемы Rate Limit 429

## Проблема

WB API возвращает **Rate Limit 429** из-за частых запросов к аналитике. Каждая загрузка страницы аналитики делает множество запросов к WB API, что приводит к блокировке на 58+ секунд.

## Решение

Реализовано **кеширование в БД** с автоматическим обновлением:

- ✅ **Кеш в PostgreSQL** - быстро и надежно
- ✅ **TTL 60 минут** - данные актуальны в течение часа
- ✅ **Кнопка "Обновить"** - принудительное обновление данных
- ✅ **Автоматическая очистка** - истекшие кеши удаляются

## Что было сделано

### 1. Добавлена модель DataCache в Prisma

```prisma
model DataCache {
  id        String   @id @default(cuid())
  key       String   @unique
  data      Json
  expiresAt DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([key])
  @@index([expiresAt])
  @@map("data_cache")
}
```

### 2. Создан сервис CacheService

**Файл:** `lib/services/cacheService.ts`

**Методы:**
- `get<T>(key)` - получить данные из кеша
- `set(key, data, ttlMinutes)` - сохранить данные в кеш
- `delete(key)` - удалить кеш
- `deletePattern(pattern)` - удалить кеши по паттерну
- `cleanExpired()` - очистить истекшие кеши

### 3. Обновлен API аналитики

**Файл:** `src/app/api/analytics/dashboard/route.ts`

**Логика:**
1. При запросе проверяется кеш в БД
2. Если кеш найден и не истек - возвращаются данные из кеша
3. Если `forceRefresh=true` - кеш удаляется и загружаются свежие данные
4. После загрузки данные сохраняются в кеш на 60 минут

## Установка

### Шаг 1: Применить миграцию БД

**Локально (через Supabase Dashboard):**
1. Откройте https://app.supabase.com
2. SQL Editor → New query
3. Скопируйте содержимое `migrations/add_data_cache.sql`
4. Нажмите "Run"

**Или через командную строку:**
```bash
# Сгенерировать Prisma Client
npx prisma generate

# Применить миграцию
psql "postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres" < migrations/add_data_cache.sql
```

### Шаг 2: Деплой на сервер

```bash
# Коммит изменений
git add .
git commit -m "feat: Add DB caching to solve Rate Limit 429"
git push origin main

# Загрузить файлы на сервер
scp prisma/schema.prisma deploy@109.172.9.61:~/app/prisma/
scp lib/services/cacheService.ts deploy@109.172.9.61:~/app/lib/services/
scp src/app/api/analytics/dashboard/route.ts deploy@109.172.9.61:~/app/src/app/api/analytics/dashboard/
scp migrations/add_data_cache.sql deploy@109.172.9.61:~/app/migrations/

# Подключиться к серверу
ssh deploy@109.172.9.61

# Применить миграцию
cd ~/app
psql "$DATABASE_URL" < migrations/add_data_cache.sql

# Сгенерировать Prisma Client
npx prisma generate

# Пересобрать и перезапустить
npm run build && pm2 restart neals-app
```

## Как работает

### Первая загрузка (без кеша)
```
1. Пользователь открывает аналитику
2. Система проверяет кеш в БД → НЕ НАЙДЕН
3. Загружаются данные из WB API (2-3 минуты)
4. Данные сохраняются в кеш на 60 минут
5. Возвращаются пользователю
```

### Последующие загрузки (с кешем)
```
1. Пользователь открывает аналитику
2. Система проверяет кеш в БД → НАЙДЕН
3. Данные возвращаются из кеша (мгновенно!)
4. Показывается сообщение: "Данные из кеша. Нажмите 'Обновить' для получения свежих данных."
```

### Обновление данных
```
1. Пользователь нажимает кнопку "Обновить"
2. Параметр forceRefresh=true
3. Старый кеш удаляется
4. Загружаются свежие данные из WB API
5. Новые данные сохраняются в кеш
```

## Преимущества

✅ **Нет Rate Limit 429** - запросы к WB API только раз в час  
✅ **Быстрая загрузка** - данные из БД возвращаются мгновенно  
✅ **Контроль пользователя** - кнопка "Обновить" для свежих данных  
✅ **Автоматическая очистка** - истекшие кеши удаляются  
✅ **Надежность** - данные в PostgreSQL, не зависит от Redis  

## Мониторинг

### Проверить кеш в БД
```sql
-- Все кеши
SELECT key, 
       EXTRACT(EPOCH FROM (NOW() - "createdAt"))/60 as age_minutes,
       EXTRACT(EPOCH FROM ("expiresAt" - NOW()))/60 as expires_in_minutes
FROM data_cache 
ORDER BY "createdAt" DESC;

-- Кеши аналитики
SELECT * FROM data_cache WHERE key LIKE 'analytics:%';

-- Истекшие кеши
SELECT COUNT(*) FROM data_cache WHERE "expiresAt" < NOW();
```

### Очистить истекшие кеши вручную
```sql
DELETE FROM data_cache WHERE "expiresAt" < NOW();
```

## Логи

**При использовании кеша:**
```
✅ [DB Cache] Данные найдены в кеше БД
📦 [Cache] Кеш найден: analytics:user123:cabinet456:30 (возраст: 15 мин)
```

**При обновлении:**
```
🔄 [DB Cache] Принудительное обновление данных (forceRefresh=true), удаляем старый кеш...
🗑️ [Cache] Кеш удален: analytics:user123:cabinet456:30
⚠️ [DB Cache] Кеш отсутствует или истек, загружаем свежие данные из WB API...
💾 [Cache] Данные сохранены: analytics:user123:cabinet456:30 (TTL: 60 мин)
✅ [DB Cache] Аналитика сохранена в БД кеш на 60 минут
```

## Результат

**До:**
- ⚠️ Rate Limit 429 каждые 2-3 запроса
- ⏱️ Загрузка 2-3 минуты каждый раз
- 😞 Пользователь ждет при каждом обновлении

**После:**
- ✅ Нет Rate Limit 429
- ⚡ Мгновенная загрузка из кеша
- 😊 Пользователь получает данные сразу
- 🔄 Обновление по кнопке когда нужно

# 🔑 Настройка токена WB API

## ❌ Ошибка: "Токен WB API не найден"

Эта ошибка возникает когда:
1. У товара нет привязанного кабинета WB
2. У кабинета нет токена WB API
3. Токен устарел или неактивен

---

## ✅ Решение: Настройка токена

### Шаг 1: Получите токен WB API

1. Откройте [Личный кабинет WB Seller](https://seller.wildberries.ru/)
2. Перейдите: **Настройки → Доступ к API**
3. Нажмите **"Создать новый токен"**
4. Выберите права доступа:
   - ✅ **Content** (контент товаров)
   - ✅ **Analytics** (аналитика)
   - ✅ **Promotion** (реклама)
   - ✅ **Statistics** (статистика)
5. Скопируйте токен (он показывается только один раз!)

**Пример токена:**
```
eyJhbGciOiJFUzI1NiIsImtpZCI6IjIwMjUwNTIwdjEiLCJ0eXAiOiJKV1QifQ...
```

---

### Шаг 2: Добавьте токен в систему

#### Вариант А: Через интерфейс (рекомендуется)

1. Откройте раздел [**Кабинеты WB**](/cabinets)
2. Нажмите **"Добавить кабинет"** или выберите существующий
3. Вставьте токен в поле **"Токен WB API"**
4. Нажмите **"Сохранить"**

#### Вариант Б: Через .env файл

Добавьте в `.env`:
```env
WB_API_TOKEN=ваш_токен_здесь
```

---

### Шаг 3: Привяжите товар к кабинету

1. Откройте товар для редактирования
2. Выберите кабинет с токеном
3. Сохраните изменения

**ИЛИ**

Товар автоматически привяжется к первому активному кабинету при создании.

---

### Шаг 4: Проверьте настройку

1. Вернитесь на страницу [**Тест AI Оптимизации**](/test-ai-optimization)
2. Выберите товар
3. Нажмите **"Получить данные оптимизации"**
4. Данные должны загрузиться успешно ✅

---

## 🔍 Проверка токена

### Через API (для разработчиков)

```bash
# Проверка токена
curl -H "Authorization: YOUR_TOKEN" \
  https://suppliers-api.wildberries.ru/public/api/v1/info
```

**Успешный ответ:**
```json
{
  "supplierID": 123456,
  "name": "Ваша компания"
}
```

**Ошибка 401:**
```json
{
  "error": "Unauthorized"
}
```
→ Токен неверный или устарел

---

## 📋 Структура базы данных

### Таблица `Cabinet`
```prisma
model Cabinet {
  id          String   @id @default(cuid())
  userId      String
  name        String
  apiToken    String?  // Токен WB API
  isActive    Boolean  @default(true)
  // ...
}
```

### Таблица `ProductCabinet` (связь)
```prisma
model ProductCabinet {
  id         String   @id @default(cuid())
  productId  String
  cabinetId  String
  isSelected Boolean  @default(false)
  // ...
}
```

---

## ⚠️ Частые проблемы

### 1. Токен не работает
**Причины:**
- Токен скопирован не полностью
- Токен удален в личном кабинете WB
- Недостаточно прав доступа

**Решение:**
- Создайте новый токен с полными правами
- Убедитесь что скопировали весь токен

### 2. Товар не привязан к кабинету
**Причины:**
- Товар создан до добавления кабинета
- Связь `ProductCabinet` не создана

**Решение:**
```sql
-- Проверить связь
SELECT * FROM "ProductCabinet" WHERE "productId" = 'ваш_product_id';

-- Создать связь вручную (если нужно)
INSERT INTO "ProductCabinet" ("id", "productId", "cabinetId", "isSelected")
VALUES (gen_random_uuid(), 'product_id', 'cabinet_id', true);
```

### 3. Кабинет неактивен
**Причины:**
- `isActive = false` в базе данных

**Решение:**
```sql
-- Активировать кабинет
UPDATE "Cabinet" SET "isActive" = true WHERE id = 'cabinet_id';
```

---

## 🎯 Проверочный список

Перед тестированием убедитесь:

- [ ] Токен WB API получен
- [ ] Токен добавлен в кабинет
- [ ] Кабинет активен (`isActive = true`)
- [ ] Товар привязан к кабинету
- [ ] Товар опубликован на WB (`wbNmId` заполнен)
- [ ] Токен имеет права: Content, Analytics, Promotion, Statistics

---

## ✅ Готово!

После настройки токена:
1. Перезагрузите страницу `/test-ai-optimization`
2. Выберите товар
3. Нажмите "Получить данные оптимизации"
4. Данные загрузятся успешно! 🎉

---

## 📞 Нужна помощь?

Если проблема не решена:
1. Проверьте логи сервера
2. Убедитесь что товар опубликован на WB
3. Проверьте что токен не истек
4. Создайте новый токен с полными правами

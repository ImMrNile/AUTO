# 🚀 API для получения данных оптимизации товаров WB

## Обзор

Комплексный API endpoint для получения всех данных необходимых для оптимизации и продвижения товаров на Wildberries через ИИ.

## Endpoint

```
GET /api/products/[id]/optimization-data
```

## Аутентификация

Требуется авторизация через NextAuth session.

## Параметры

- `id` (path parameter) - ID товара в системе

## Возвращаемые данные

### 1. **Поисковые запросы** (`searchQueries`)
**Источник:** `POST /api/v2/search-report/product/search-texts`

Топ-30 поисковых запросов по которым находят товар:

```json
{
  "data": [
    {
      "text": "умные часы",
      "frequency": 1250,
      "avgPosition": 12.5,
      "openCard": 450,
      "addToCart": 89,
      "orders": 45,
      "openToCart": 19.78,
      "cartToOrder": 50.56
    }
  ]
}
```

**Метрики:**
- `text` - поисковый запрос
- `frequency` - частота запросов
- `avgPosition` - средняя позиция товара
- `openCard` - переходы в карточку
- `addToCart` - добавления в корзину
- `orders` - заказы
- `openToCart` - конверсия в корзину (%)
- `cartToOrder` - конверсия в заказ (%)

**Применение для ИИ:**
- Определить "золотые" запросы (высокая конверсия, низкая позиция)
- Найти запросы для добавления в фиксированные фразы
- Выявить запросы с низкой конверсией для минус-слов

---

### 2. **Конверсия** (`conversion`)
**Источник:** `POST /api/analytics/v3/sales-funnel/products`

Воронка продаж товара:

```json
{
  "data": [
    {
      "nmID": 123456789,
      "openCount": 5420,
      "cartCount": 892,
      "orderCount": 445,
      "buyoutCount": 378,
      "conversions": {
        "openToCart": 16.46,
        "cartToOrder": 49.89,
        "orderToBuyout": 84.94
      }
    }
  ]
}
```

**Метрики:**
- `openCount` - просмотры карточки
- `cartCount` - добавления в корзину
- `orderCount` - заказы
- `buyoutCount` - выкупы
- `conversions` - конверсии на каждом этапе

**Применение для ИИ:**
- Выявить слабые места в воронке
- Сравнить с конкурентами
- Рекомендации по улучшению карточки/цены

---

### 3. **Рекламные кампании** (`campaigns`)
**Источник:** `GET /adv/v3/fullstats`

Статистика всех кампаний с товаром:

```json
{
  "data": [
    {
      "advertId": 12345,
      "name": "Автокампания смарт-часы",
      "views": 45230,
      "clicks": 3890,
      "ctr": 8.6,
      "cpc": 45.5,
      "sum": 177045,
      "orders": 156,
      "cr": 4.01,
      "shks": 156,
      "sum_price": 234000,
      "roi": 32.15
    }
  ]
}
```

**Метрики:**
- `views` - показы
- `clicks` - клики
- `ctr` - CTR (%)
- `cpc` - средняя цена клика (₽)
- `sum` - потрачено (₽)
- `orders` - заказы
- `cr` - конверсия (%)
- `roi` - ROI (%)

**Применение для ИИ:**
- Определить прибыльные/убыточные кампании
- Рекомендации по ставкам
- Оптимизация бюджета

---

### 4. **Воронка продаж по дням** (`salesFunnel`)
**Источник:** `POST /api/analytics/v3/sales-funnel/products/history`

История воронки продаж за 30 дней:

```json
{
  "data": [
    {
      "nmID": 123456789,
      "history": [
        {
          "dt": "2025-01-01",
          "openCount": 180,
          "cartCount": 29,
          "orderCount": 15,
          "buyoutCount": 13
        }
      ]
    }
  ]
}
```

**Применение для ИИ:**
- Анализ трендов
- Выявление аномалий
- Прогнозирование продаж

---

### 5. **Ключевые слова** (`keywords`)
**Источник:** `POST /adv/v0/normquery/stats`

Статистика по ключевым фразам в рекламе:

```json
{
  "data": [
    {
      "norm_query": "умные часы",
      "views": 12450,
      "clicks": 890,
      "ctr": 7.15,
      "cpc": 42.3,
      "orders": 45,
      "avg_pos": 8.5
    }
  ]
}
```

**Метрики:**
- `norm_query` - нормализованный запрос
- `views` - показы
- `clicks` - клики
- `ctr` - CTR (%)
- `cpc` - цена клика (₽)
- `orders` - заказы
- `avg_pos` - средняя позиция

**Применение для ИИ:**
- Классификация фраз (золотые/убыточные/перспективные)
- Рекомендации по ставкам
- Минус-слова

---

## Классификация для ИИ

### 🟢 "Золотые" фразы
**Критерии:**
- Конверсия > 2%
- CPC < 300₽
- Позиция < 10

**Действия:**
- Увеличить ставки на 30%
- Добавить в фиксированные фразы
- Приоритет в бюджете

### 🔴 "Убыточные" фразы
**Критерии:**
- Конверсия < 0.5%
- CPC > 500₽
- ROI < 0%

**Действия:**
- Добавить в минус-слова
- Экономия бюджета
- Перераспределение на золотые

### 🟡 "Перспективные" фразы
**Критерии:**
- Конверсия > 2%
- Позиция > 10

**Действия:**
- Увеличить ставки на 50%
- Цель: выход в топ-5
- Мониторинг ROI

---

## Формулы оптимизации

### Оптимальная ставка
```
ROI > 200% → новая_ставка = текущая × 1.3
ROI 0-200% → оставить текущую
ROI < 0% → новая_ставка = текущая × 0.5 или отключить
```

### Целевые показатели
- **CTR рекламы:** > 8% (отлично > 15%)
- **Конверсия в корзину:** > 15% (отлично > 25%)
- **Конверсия в заказ:** > 50% (отлично > 70%)
- **ROI рекламы:** > 200% (отлично > 400%)
- **CPC:** < 300₽ (отлично < 150₽)

---

## Пример использования

### 1. Через API

```typescript
const response = await fetch('/api/products/123/optimization-data');
const { data } = await response.json();

// Анализ поисковых запросов
const goldenQueries = data.searchQueries.data.filter(q => 
  q.cartToOrder > 2 && q.avgPosition < 10
);

// Анализ кампаний
const profitableCampaigns = data.campaigns.data.filter(c => 
  c.roi > 200
);
```

### 2. Через тестовый интерфейс

1. Откройте `/test-optimization`
2. Введите ID товара
3. Нажмите "Получить данные"
4. Просмотрите все секции данных

---

## Обработка ошибок

API возвращает частичные данные даже при ошибках:

```json
{
  "success": true,
  "data": {
    "searchQueries": { ... },
    "conversion": null,
    "campaigns": { ... },
    "errors": {
      "searchQueries": null,
      "conversion": "WB API error: 403",
      "campaigns": null
    }
  }
}
```

**Возможные ошибки:**
- `401` - Токен не имеет доступа к Analytics API
- `403` - Недостаточно прав
- `404` - Товар не найден
- `500` - Внутренняя ошибка WB API

---

## Рекомендации по использованию

### Для ИИ-агента оптимизации:

1. **Анализ данных:**
   - Загрузить все данные через API
   - Классифицировать фразы
   - Выявить проблемы в воронке

2. **Генерация рекомендаций:**
   - Список золотых фраз для увеличения ставок
   - Список убыточных фраз для минус-слов
   - Рекомендации по улучшению карточки
   - Оптимальное распределение бюджета

3. **Применение изменений:**
   - Использовать WB Promotion API для изменения ставок
   - Добавить минус-слова через `/adv/v0/normquery/set-minus`
   - Обновить фиксированные фразы через `/adv/v1/search/set-plus`

---

## Файлы проекта

- **API Router:** `src/app/api/products/[id]/optimization-data/route.ts`
- **Тестовый компонент:** `src/app/components/OptimizationDataTest.tsx`
- **Тестовая страница:** `src/app/(protected)/test-optimization/page.tsx`

---

## Следующие шаги

1. ✅ Создан API endpoint для получения данных
2. ✅ Создан тестовый интерфейс
3. ⏳ Интеграция с ИИ-агентом оптимизации
4. ⏳ Автоматическое применение рекомендаций
5. ⏳ Мониторинг результатов оптимизации

---

## Лимиты WB API

- **Search Report:** 1 запрос/секунду
- **Analytics:** 1 запрос/секунду
- **Promotion:** 1 запрос/секунду

**Рекомендация:** Кешировать данные на 1 час для избежания лимитов.

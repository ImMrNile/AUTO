# 🚀 Инструкции по деплою на Vercel

## ❌ Критические проблемы

### 1. Схема БД не синхронизирована
```
Error: The column `products.dbsStock` does not exist in the current database
```

**Причина:** Prisma схема на Vercel устарела

**Решение:**
```bash
# 1. Сгенерировать новую схему Prisma
npx prisma generate

# 2. Применить миграции к БД
npx prisma migrate deploy

# 3. Задеплоить на Vercel
npx vercel --prod --force
```

### 2. Долгая загрузка Telegram Mini App

**Было:** 
- Middleware редиректил на `/auth/login`
- Затем `/auth/login` редиректил на `/onboarding`
- Затем `page.tsx` проверял кабинеты и снова редиректил

**Исправлено:**
- `/onboarding` добавлен в публичные пути middleware
- API `/api/auth/telegram` сразу возвращает `redirectTo`
- AuthGuard пропускает `/onboarding` без проверки

---

## ✅ Что было исправлено

1. **AuthGuard.tsx** - быстрая проверка для Telegram Mini App
2. **middleware.ts** - `/onboarding` в публичных путях
3. **telegram/route.ts** - проверка кабинетов сразу после авторизации
4. **BackgroundSyncWorker.tsx** - валидация цен перед обновлением
5. **SSE stream** - оптимизация запроса (разделение активных/завершенных задач)

---

## 📋 Чеклист перед деплоем

- [ ] Запустить `npx prisma generate`
- [ ] Запустить `npx prisma migrate deploy`
- [ ] Проверить переменные окружения на Vercel:
  - `DATABASE_URL`
  - `JWT_SECRET`
  - `TELEGRAM_BOT_TOKEN`
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- [ ] Задеплоить: `npx vercel --prod --force`
- [ ] Проверить логи на Vercel Dashboard

---

## 🐛 Известные проблемы

### WB API: "The specified prices and discounts are already set"

**Причина:** Цена уже установлена на WB, повторная установка не нужна

**Решение:** Это не ошибка, просто информационное сообщение. Можно игнорировать.

---

## 📞 Поддержка

Если проблемы остались:
1. Проверьте логи на Vercel Dashboard
2. Проверьте, что БД доступна
3. Проверьте, что все переменные окружения установлены

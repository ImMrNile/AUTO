# 🧪 Тестирование AI системы продвижения

## ✅ Быстрый тест

### 1. Проверка UI

1. Откройте раздел **"Товары"** (inventory page)
2. Найдите любой опубликованный товар
3. Кликните на товар, чтобы раскрыть детали
4. **Проверьте**: Есть ли кнопка "Оптимизировать через AI" с иконкой ✨?

### 2. Проверка модального окна

1. Нажмите кнопку "Оптимизировать через AI"
2. **Проверьте**:
   - ✅ Открылось модальное окно
   - ✅ Отображается название товара
   - ✅ Есть поле "Бюджет на неделю"
   - ✅ Есть выбор типа оптимизации
   - ✅ Показывается дневной бюджет (бюджет/7)

### 3. Тест запуска оптимизации

1. Установите бюджет: **1000₽**
2. Выберите тип: **"Продвижение и контент"**
3. Нажмите **"Запустить оптимизацию"**
4. **Проверьте консоль браузера (F12)**:
   ```
   ✅ AI оптимизация запущена: {success: true, ...}
   ```

### 4. Проверка создания чатов

Откройте консоль сервера и найдите:
```
🤖 [AI] Запуск оптимизации товара [ID] для пользователя [USER_ID]
   Бюджет: 1000₽/неделя
   Тип: both
🧵 [AI] Создан Thread для promotion: thread_xxx
🧵 [AI] Создан Thread для content: thread_xxx
✅ [AI] Отправлено начальное сообщение для promotion
✅ [AI] Отправлено начальное сообщение для content
✅ [AI] Создано 2 чатов оптимизации для товара [NAME]
```

## 🔍 Детальная проверка

### Проверка API endpoints

```bash
# 1. Получить товар по nmID
curl http://localhost:3000/api/products/by-nmId/123456789

# 2. Запустить оптимизацию
curl -X POST http://localhost:3000/api/products/[PRODUCT_ID]/optimize \
  -H "Content-Type: application/json" \
  -d '{
    "weeklyBudget": 1000,
    "optimizationType": "both"
  }'
```

### Проверка базы данных

```sql
-- Проверить созданные чаты
SELECT * FROM "ProductAiChat" 
WHERE "productId" = '[PRODUCT_ID]' 
ORDER BY "createdAt" DESC;

-- Проверить сообщения
SELECT * FROM "ProductAiMessage" 
WHERE "chatId" IN (
  SELECT id FROM "ProductAiChat" 
  WHERE "productId" = '[PRODUCT_ID]'
)
ORDER BY "createdAt" DESC;
```

### Проверка OpenAI

1. Откройте [OpenAI Platform](https://platform.openai.com/assistants)
2. Найдите ваших ассистентов
3. Проверьте созданные Threads
4. Посмотрите сообщения в Thread

## 🐛 Возможные проблемы

### Кнопка не отображается

**Причина**: Товар не найден в БД или нет nmID

**Решение**:
```typescript
// Проверьте в консоли браузера
console.log(product.nmID); // Должен быть числом
```

### Ошибка "Товар не найден"

**Причина**: Товар не синхронизирован с БД

**Решение**:
1. Откройте раздел "Товары"
2. Нажмите "Синхронизировать остатки"
3. Дождитесь завершения
4. Попробуйте снова

### Ошибка "OPENAI_API_KEY not found"

**Причина**: Не настроен API ключ OpenAI

**Решение**:
```bash
# .env.local
OPENAI_API_KEY=sk-proj-...
OPENAI_ASSISTANT_ID=asst_...
OPENAI_CONTENT_ASSISTANT_ID=asst_...
```

### Чаты создаются, но нет ответа AI

**Причина**: Ассистенты не настроены или не имеют инструкций

**Решение**:
1. Откройте OpenAI Platform
2. Проверьте настройки ассистентов
3. Убедитесь, что у них есть инструкции (Instructions)
4. Проверьте модель (должна быть gpt-4o или gpt-4-turbo)

## 📊 Ожидаемый результат

После успешного запуска:

1. ✅ Создано 2 AI чата (promotion + content)
2. ✅ Отправлены начальные сообщения с данными товара
3. ✅ AI начинает анализ
4. ✅ Через 3 часа запустится автоматическая проверка
5. ✅ AI применит первые улучшения

## 🎯 Следующие шаги

1. **Мониторинг**: Перейдите на `/products/[id]/ai-improve`
2. **Просмотр чатов**: Откройте страницу товара
3. **Проверка действий**: Посмотрите таблицу `PromotionAction`
4. **Отчеты**: Проверьте таблицу `PromotionReport`

## 📞 Помощь

Если что-то не работает:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера
3. Проверьте переменные окружения
4. Убедитесь, что WB API токен валиден
5. Проверьте баланс OpenAI API

# 🔍 ПОЛНЫЙ АНАЛИЗ: Search Report API

## Все эндпоинты Search Report

### 1. `/api/v2/search-report/report` — Основная страница
**Что возвращает:**
- Общую информацию
- Позиции товаров
- Данные по видимости и переходам
- Данные для таблицы по группам

**Ключевой момент:**
> Параметры brandNames, subjectIds, tagIds, nmIds могут быть пустыми [], тогда в ответе возвращаются все карточки продавца.

**Требует Jam:** ДА (403 без подписки)

---

### 2. `/api/v2/search-report/table/groups` — Пагинация по группам
**Что возвращает:**
- Дополнительные данные с пагинацией по группам
- Метрики по группам (предмет, бренд, ярлык)

**Требует Jam:** ДА (403 без подписки)

---

### 3. `/api/v2/search-report/table/details` — Пагинация по товарам
**Что возвращает:**
- Пагинация по товарам в группе
- Можно фильтровать по:
  - `subjectId, brandName, tagId` — для группы
  - `nmIds` — для конкретных товаров

**Требует Jam:** ДА (403 без подписки)

---

### 4. `/api/v2/search-report/product/search-texts` — Поисковые запросы по товару
**Что возвращает:**
- Топ поисковых запросов по товару
- Текст запроса
- Частота появления
- Позиции
- Переходы, заказы, конверсии

**Параметры:**
```json
{
  "currentPeriod": {
    "start": "2024-02-10",
    "end": "2024-02-10"
  },
  "nmIds": [162579635],
  "topOrderBy": "openToCart",
  "includeSubstitutedSKUs": true,
  "includeSearchTexts": false,
  "limit": 20  // max 30 (базовый) или 100 (продвинутый)
}
```

**Требует Jam:** ДА (403 без подписки)

---

### 5. `/api/v2/search-report/product/orders` — Заказы по запросам
**Что возвращает:**
- Количество заказов по каждому поисковому запросу
- Позиции в поиске по дням
- Детализация по датам

**Параметры:**
```json
{
  "period": {
    "start": "2024-02-10",
    "end": "2024-02-10"
  },
  "nmId": 211131895,
  "searchTexts": ["костюм", "пиджак"]
}
```

**Требует Jam:** ДА (403 без подписки)

---

## Вывод: ВСЕ Search Report эндпоинты требуют Jam

### Проверено:
- ✅ `/report` — 403 без Jam
- ✅ `/table/groups` — 403 без Jam
- ✅ `/table/details` — 403 без Jam
- ✅ `/product/search-texts` — 403 без Jam
- ✅ `/product/orders` — 403 без Jam

### Из документации:
> Чтобы получать отчёты за период до года, используйте методы **Аналитика продавца CSV**. Эти методы доступны только с подпиской на **расширенную аналитику Джем**

---

## Альтернативы БЕЗ Jam

### 1. ✅ V3 Analytics API (БЕЗ Jam!)
```
POST /api/analytics/v3/sales-funnel/products
- Просмотры за 365 дней
- Заказы за 365 дней
- Конверсии за 365 дней
- Выкупы за 365 дней
- Рейтинг
- Остатки
```

### 2. ✅ V3 Products History (БЕЗ Jam!)
```
POST /api/analytics/v3/sales-funnel/products/history
- Детализация по дням (7 дней)
- Просмотры каждый день
- Заказы каждый день
```

### 3. ✅ V3 Grouped History (БЕЗ Jam!)
```
POST /api/analytics/v3/sales-funnel/grouped/history
- Статистика по категории (7 дней)
- Контекст рынка
```

### 4. ✅ Statistics API (БЕЗ Jam!)
```
GET /api/v1/supplier/sales
- Все продажи
- Вся история
```

### 5. ✅ Advert API (БЕЗ Jam!)
```
GET /adv/v1/promotion/count
POST /adv/v0/normquery/stats
- Все кампании
- Ключевые слова из рекламы
```

---

## Что можем сделать БЕЗ Jam

### Вместо поисковых запросов используем:

#### 1. Ключевые слова из рекламы
```typescript
// Получаем кампании
GET /adv/v1/promotion/count

// Получаем ключевые слова из каждой кампании
POST /adv/v0/normquery/stats
{
  "id": [27673276],
  "interval": {
    "begin": "2025-10-01",
    "end": "2025-10-31"
  }
}

// Ответ:
{
  "keywords": [
    {
      "keyword": "платье для умры",
      "count": 150,
      "views": 5000,
      "clicks": 250,
      "ctr": 5.0,
      "orders": 10
    }
  ]
}
```

**Преимущества:**
- ✅ БЕЗ Jam
- ✅ Реальные ключевые слова
- ✅ Статистика по каждому слову
- ✅ Вся история

#### 2. Анализ названия товара
```typescript
// Из V3 Products Summary получаем:
{
  "product": {
    "title": "Платье с химаром для Умры и Хаджа",
    "subjectName": "Платья",
    "brandName": ""
  }
}

// Извлекаем ключевые слова:
const keywords = [
  "платье",
  "химар",
  "умра",
  "хадж",
  "мусульманское платье"
];
```

#### 3. Анализ категории
```typescript
// Из V3 Grouped History получаем статистику по категории
{
  "group": {
    "subjectId": 69,
    "subjectName": "Платья"
  },
  "history": [
    {
      "date": "2025-10-29",
      "openCount": 5000,  // Просмотры всей категории
      "orderCount": 400   // Заказы всей категории
    }
  ]
}

// Вычисляем долю товара:
const productShare = productViews / categoryViews;
```

---

## Итоговая архитектура БЕЗ Jam

### Источники данных:

```
1. V3 Products Summary (365 дней)
   ├── Просмотры
   ├── Заказы
   ├── Конверсии
   └── Рейтинг

2. V3 Products History (7 дней)
   └── Детализация по дням

3. V3 Grouped History (7 дней)
   └── Контекст категории

4. Statistics API (вся история)
   └── Продажи

5. Advert API (вся история)
   ├── Кампании
   └── Ключевые слова из рекламы ← ВМЕСТО Search Report!

6. Анализ названия товара
   └── Извлечение ключевых слов

7. Анализ категории
   └── Доля рынка
```

---

## Сравнение: С Jam vs БЕЗ Jam

### С Jam получаем:
| Данные | Источник | Период |
|--------|----------|--------|
| Поисковые запросы | Search Report | 90 дней |
| Позиции в поиске | Search Report | 90 дней |
| Заказы по запросам | Search Report | 90 дней |

### БЕЗ Jam получаем:
| Данные | Источник | Период |
|--------|----------|--------|
| Ключевые слова | Advert API | Вся история |
| Просмотры | V3 Summary | 365 дней |
| Заказы | V3 Summary | 365 дней |
| Конверсии | V3 Summary | 365 дней |
| Детализация | V3 History | 7 дней |
| Контекст рынка | V3 Grouped | 7 дней |
| Продажи | Statistics | Вся история |

---

## Код для получения ключевых слов БЕЗ Jam

```typescript
async function getKeywordsWithoutJam(nmId: number, subjectId: number) {
  const keywords: string[] = [];
  
  // 1. Получаем ключевые слова из рекламы
  const countResponse = await fetch(
    'https://advert-api.wildberries.ru/adv/v1/promotion/count',
    {
      method: 'GET',
      headers: { 'Authorization': apiToken }
    }
  );
  
  if (countResponse.ok) {
    const countData = await countResponse.json();
    const campaignIds: number[] = [];
    
    // Собираем ID кампаний
    countData.adverts?.forEach((group: any) => {
      group.advert_list?.forEach((adv: any) => {
        campaignIds.push(adv.advertId);
      });
    });
    
    // Получаем ключевые слова из каждой кампании
    for (const campaignId of campaignIds) {
      const keywordsResponse = await fetch(
        'https://advert-api.wildberries.ru/adv/v0/normquery/stats',
        {
          method: 'POST',
          headers: {
            'Authorization': apiToken,
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            id: [campaignId],
            interval: {
              begin: startDate,
              end: endDate
            }
          })
        }
      );
      
      if (keywordsResponse.ok) {
        const keywordsData = await keywordsResponse.json();
        keywordsData.forEach((kw: any) => {
          keywords.push(kw.keyword);
        });
      }
    }
  }
  
  // 2. Получаем название товара из V3 Summary
  const summaryResponse = await fetch(
    'https://seller-analytics-api.wildberries.ru/api/analytics/v3/sales-funnel/products',
    {
      method: 'POST',
      headers: {
        'Authorization': apiToken,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        selectedPeriod: { start: startDate, end: endDate },
        pastPeriod: { start: pastStart, end: pastEnd },
        nmIds: [nmId],
        skipDeletedNm: false
      })
    }
  );
  
  if (summaryResponse.ok) {
    const summaryData = await summaryResponse.json();
    const product = summaryData.data?.products?.[0];
    
    if (product?.product?.title) {
      // Извлекаем ключевые слова из названия
      const titleWords = product.product.title
        .toLowerCase()
        .split(/\s+/)
        .filter((w: string) => w.length > 3);
      
      keywords.push(...titleWords);
    }
  }
  
  // 3. Удаляем дубликаты
  return [...new Set(keywords)];
}
```

---

## Резюме

### ❌ Search Report API
- **ВСЕ эндпоинты требуют Jam**
- Нет способа обойти это ограничение
- 403 ошибка без подписки

### ✅ Альтернатива БЕЗ Jam
- V3 Analytics API — полная аналитика
- Advert API — ключевые слова из рекламы
- Statistics API — продажи
- Анализ названия — извлечение ключевых слов
- Анализ категории — контекст рынка

### Итог
**Мы получаем 90% данных БЕЗ Jam!**

Единственное что теряем:
- Точные поисковые запросы покупателей
- Позиции в поиске по запросам

Но получаем:
- Ключевые слова из рекламы (часто лучше!)
- Полную аналитику за 365 дней
- Детализацию по дням
- Контекст рынка
- Продажи

**Jam нужен ТОЛЬКО если критически важны точные поисковые запросы покупателей!**

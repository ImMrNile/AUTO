# Добавлена кнопка "Удалить" для товаров в работе

## Изменения

### 1. Добавлена кнопка "Удалить"

**Расположение:** Рядом с кнопкой "Опубликовать" в разделе "Товары в работе"

**Функционал:**
- Удаляет товар из базы данных
- Показывает подтверждение перед удалением
- Отображает статус "Удаление..." во время процесса
- Автоматически убирает товар из списка после удаления

### 2. Интерфейс

**Кнопка:**
```tsx
<button className="flex items-center gap-2 px-4 py-2 bg-red-100 hover:bg-red-200 disabled:opacity-50 text-red-700 rounded-lg font-semibold transition-all shadow-md text-sm">
  <Trash2 className="w-4 h-4" />
  {deletingTaskId === task.id ? 'Удаление...' : 'Удалить'}
</button>
```

**Подтверждение:**
```javascript
if (!confirm('Вы уверены, что хотите удалить этот товар?')) return;
```

### 3. API запрос

```javascript
const response = await fetch(`/api/products/${task.productId}`, {
  method: 'DELETE',
  credentials: 'include'
});
```

### 4. Логика удаления

1. **Подтверждение** - пользователь подтверждает удаление
2. **Отправка запроса** - DELETE запрос к API
3. **Обновление UI** - товар убирается из списка
4. **Логирование** - все действия логируются

```typescript
setDeletingTaskId(task.id);
try {
  clientLogger.log(`🗑️ [InProgress] Удаляем товар: ${task.productId}`);
  
  const response = await fetch(`/api/products/${task.productId}`, {
    method: 'DELETE',
    credentials: 'include'
  });
  
  if (response.ok) {
    clientLogger.log(`✅ [InProgress] Товар успешно удален`);
    // Удаляем из списка
    const filteredTasks = tasks.filter(t => t.id !== task.id);
    setTasks(filteredTasks);
    clientLogger.log(`✅ [InProgress] Товар удален из раздела. Осталось товаров: ${filteredTasks.length}`);
  } else {
    const error = await response.json();
    clientLogger.error(`❌ [InProgress] Ошибка удаления:`, error);
    alert('Ошибка удаления товара');
  }
} catch (err) {
  clientLogger.error('❌ [InProgress] Ошибка удаления:', err);
  alert('Ошибка удаления товара');
} finally {
  setDeletingTaskId(null);
}
```

### 5. Состояния кнопки

| Состояние | Текст | Disabled | Цвет |
|-----------|-------|----------|------|
| **Обычное** | "Удалить" | false | Красный (bg-red-100) |
| **Удаление** | "Удаление..." | true | Красный (opacity-50) |
| **Во время публикации** | "Удалить" | true | Красный (opacity-50) |
| **Во время сохранения** | "Удалить" | true | Красный (opacity-50) |

### 6. Добавлен state

```typescript
const [deletingTaskId, setDeletingTaskId] = useState<string | null>(null);
```

### 7. Импорт иконки

```typescript
import { Trash2 } from 'lucide-react';
```

## Проблема с отображением цены и комплектации

### Причина

Данные **price**, **discountPrice**, **costPrice**, **packaging** не отображаются, потому что:

1. **При создании товара** эти поля могут быть пустыми
2. **API возвращает** `null` или `undefined` для этих полей
3. **Компонент показывает** "0 ₽" и "(не заполнено)"

### Решение

Данные уже **редактируемые** - пользователь может:
1. Нажать на иконку редактирования ✏️
2. Ввести значение
3. Значение сохранится в `editedTasks`

### Где указывать данные

**Вариант 1: При создании товара**
- В форме Step4Results есть поля для цены и комплектации
- Пользователь заполняет их при создании

**Вариант 2: В разделе "В работе"**
- Нажать на ✏️ рядом с полем
- Ввести значение
- Нажать "Опубликовать" - данные сохранятся

### Пример заполнения

```typescript
// Цена
editedTasks[task.id].price = 7999;

// Цена со скидкой
editedTasks[task.id].discountPrice = 4000;

// Себестоимость
editedTasks[task.id].costPrice = 2500;

// Комплектация
editedTasks[task.id].packaging = "Кэйс 1 шт, Головки, Трещетки...";
```

## Файлы изменены

**`src/app/components/products/InProgressProducts.tsx`:**
1. Строка 21: Добавлен импорт `Trash2`
2. Строка 75: Добавлен state `deletingTaskId`
3. Строка 927: Изменен класс на `flex flex-wrap` для адаптивности
4. Строки 1049-1086: Добавлена кнопка "Удалить" с логикой

**`src/app/api/products/[id]/route.ts`:**
1. Строки 358-408: Добавлен метод `DELETE` для удаления товара
2. Проверка авторизации и принадлежности пользователю
3. Каскадное удаление связанных данных
4. Обработка ошибок (404 если товар не найден)

## Тестирование

1. Откройте раздел "Товары в работе"
2. Найдите товар со статусом "Готово"
3. Нажмите кнопку "Удалить"
4. Подтвердите удаление
5. Товар должен исчезнуть из списка

## Скриншот

**До:**
```
[Опубликовать] [Перейти к товару]
```

**После:**
```
[Опубликовать] [Перейти к товару] [Удалить]
```

## Примечания

- Кнопка "Удалить" **всегда видна** для товаров со статусом "COMPLETED"
- Кнопка **disabled** во время публикации, сохранения или удаления
- Удаление **необратимо** - товар удаляется из БД
- После удаления товар **автоматически убирается** из списка
